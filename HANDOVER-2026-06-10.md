# Handover — ForgeOS roster DB, supplier contacts, all-archetype re-run + universality/accuracy roadmap
**2026-06-10 · repo `CentaurOS-oxccu-efuel` · HEAD `e062e8c26` · branch oxccu-efuel**
**(Also copied to ~/Downloads/handovers/2026-06-10T05-47-e062e8c26.md — but ~/Downloads is macOS-TCC-blocked for the shell; THIS repo copy is the reliable one.)**

> Audience: another terminal (Claude Code / OpenCode) picking up this engine. Read top-to-bottom before touching the chain. Push is HELD. British spelling. Tristan does NOT run terminal commands — the agent does everything.

---

## 0. TL;DR — what happened this session
1. **Free-edition tier-masking leak-gate closed to 0** (committed `e062e8c26`). Render free → grep every supplier/website = 0 (was 37 brands + 5 sites).
2. **Built a cross-archetype ROSTER + SUPPLIER learning DB** in `~/.forge-truth/forge-truth.db` (Tristan: track potential fractional execs + suppliers; compute overlap to prioritise recruiting).
3. **Researched supplier CONTACTS** (name/role/email) for 36 recurring suppliers — web-grounded, anti-hallucination. Output `SUPPLIER-CONTACTS.md` + written to `companies.contact_*` + `contacts_json`.
4. **Re-ran ALL 8 archetypes on the latest engine** (Tristan: "all reports on the very latest version, no engine drift"). 3/8 clean, 5/8 failed quality gates. All 8 now have advisor data → harvested into the roster DB.

---

## 1. Current state — what's working
- **Engine = THIS repo** (`/Users/tristanfischer/Developer/CentaurOS-oxccu-efuel`, HEAD `e062e8c26`). NEWER than the "main" repo on render+chain+dossier-quality. Run everything here. (See §6 drift warning.)
- **Tier masking** (`DOSSIER_TIER=free|paid`, default paid): free leak-gate = 0. Paid unchanged, gate-11 PASS.
- **Roster learning DB** (`~/.forge-truth/forge-truth.db`): `fractional_execs` (126 canonical roles), `project_exec_needs` (185), `project_supplier_needs` (275), `companies.contacts_json` (36 researched contacts), 309 companies archetype-tagged.
- **Reports re-run** for all 8 → `out/rerun-<arch>/` (state.json + chain-v2.pdf + chain.log + AUDIT mds).

### Re-run gate results (the honest universality scoreboard)
| Archetype | Exit | Meaning | Advisors |
|---|---|---|---|
| co2_mineralisation | 0 | clean | 12 |
| e_fuel_synthesis (SAF) | 0 | clean (gates pass) | 8 |
| haps | 0 | clean | 10 |
| compute_heat_module | 11 | layout overlap | 8 |
| energy_storage (BESS) | 10 | BoM quality | 10 |
| vertical_farm | 10 | BoM quality | 11 |
| satellite_smallsat | 10 | BoM quality | 10 |
| edge_ai_server | 18 | cross-page numeric drift | 8 |

**Only 3/8 pass cleanly on one universal engine — this is the core universality gap.**

---

## 2. What is MISSING / open threads (tracked tasks)
- **#43 — engine-quality gate failures (accuracy):** compute_heat=11 (layout), BESS/VF/smallsat=10 (BoM: orphaned macros / cover≠Σ sub-totals / line totals), edge_ai=18 (cross-page numeric). Diagnose from `out/rerun-<arch>/chain.log` + `<pdf>.AUDIT-*.md`. Fix UNIVERSALLY (renderer/emitter), add a regression invariant per fix (CLAUDE.md rule #11).
- **#41 — two-repo engine drift:** `CentaurOS-oxccu-efuel` (this, newest) vs `CentaurOS created 260126 1435` (main trunk, has 1 extra class graph + an e_fuel-physics fix this repo lacks). Consolidate to ONE engine. `git cat-file -e <sha>` is MISLEADING (object exists ≠ in HEAD); use `git merge-base --is-ancestor` or a line-count diff.
- **Supabase push of the roster DB:** learning DB lives in forge-truth.db (correct per Tristan's 29-Apr rule). The app read-surface (Supabase project `jyarhvinengfyrwgtskq`) is NOT yet populated — needs a push script (the only sanctioned forge-truth.db → Supabase bridge).
- **"other" exec discipline bucket too coarse:** 43 role variants fell into `other` (`disciplineOf()` in `scripts/harvest-roster-to-db.mjs` needs buckets for embedded/software, fire-protection, controls-software, domain-specialist).
- **Dräger contact discrepancy:** two runs disagree (Sonnet=Lawrie Kerr/offshore vs Gemini=Megan Hine/Safe-Energy-Transition). Confirm on LinkedIn.

---

## 3. Where things live (files + DB)
**New scripts (uncommitted, untracked):**
- `scripts/harvest-roster-to-db.mjs` — dossier states → fractional_execs + project_exec_needs + project_supplier_needs (+ tag suppliers into existing `companies`). Idempotent per archetype. `npx tsx scripts/harvest-roster-to-db.mjs <arch>=<state.json> ...`
- `scripts/roster-report.mjs` — read-only → `ROSTER-OVERLAP.md`.
- `scripts/harvest-roster-overlap.mjs` — file-based pre-DB version.
- `scripts/supplier-contacts.mjs` — `research` (Gemini 3.5 Flash `:online` via OpenRouter) + `parse` (raw → DB + `SUPPLIER-CONTACTS.md`).
- `scripts/rerun-all-archetypes.sh` — re-run N archetypes through the chain, ≤3 concurrent, per-arch logs + `out/rerun-status.txt`. **De-facto all-archetype regression harness (see §4).**

**Data:** `~/.forge-truth/forge-truth.db` (learning DB: parts + suppliers + NEW exec/need tables). `briefs-rerun/<arch>.md` (the 8 briefs). `out/rerun-<arch>/` (fresh dossiers). `SUPPLIER-CONTACTS.md`/`.html`/`-raw.txt`, `ROSTER-OVERLAP.md`/`.html`.

**Uncommitted tracked edits (separate workstreams — do not blow away):** `scripts/serial-design-chain-v2.tsx`, `src/.../brief/_components/brief-editor.tsx`, `src/lib/pdf-engine-v2/radical/module-paragraph-llm.ts`, `src/lib/supabase/middleware.ts`, `next-env.d.ts`.

---

## 4. Suggestions — making the system more UNIVERSAL
1. **Make "all-archetype gate-pass rate" the standing universality KPI.** `scripts/rerun-all-archetypes.sh` already runs every archetype through the one chain → a pass/fail board. Formalise it (nightly, all registered archetypes, dashboard exit codes). Today = **3/8 clean** — that IS the universality score. Drive to 8/8.
2. **Collapse the hand-class-graph vs universal-auto-planner split.** 4 archetypes use hand graphs (bess, vf, fuel-cell, vehicle-battery); the rest use `UNIVERSAL_AUTO_PLAN` (default-on since 2026-06-03). Hand graphs are stale/buggy (HAPS hand-wiring bugs; "universal machinery built but inert" history). Goal: auto-planner good enough that hand graphs are unnecessary — or auto-validate hand graphs against it. Per Exp-A the lone wall was engineering-plausibility SIZING → per-class-FAMILY sizing plug-ins, not per-class hand graphs.
3. **Close the growing-DB writeback gaps (the self-learning pillar).** Per CLAUDE.md: `pretraining_extracted_parts` has writeback (✓), but **specs / standards / suppliers / products have NO writeback path and are NOT read at runtime** (baked 2026-05-18 TS snapshots in `class-reference-graphs/*.ts`). Wire live DB-first reads + write-backs for all four, mirroring `persist-web-fallback.ts` (suppliers) + the parts RAG. This is what lets the engine improve on unseen classes without code edits.
4. **Build on-the-fly class-data self-generation (the real BOM-coverage P1).** AIM principle: for a NEW/unknown class, DB-first → on-miss search Internet + own training → VERIFY exists → writeback (class-tagged) → read. Universal, not per-class hand-coding. Guard: verify own-generated MPNs/specs before writeback (gate-20 hallucination risk).
5. **The roster/supplier overlap is itself a universality instrument.** It shows which DISCIPLINES recur across all 8 (Mechanical 8/8, Electrical 8/8, I&C 7/8, Process 6/8) and which parts/suppliers recur — i.e. where universal sizing/data investment pays off across the most archetypes.

---

## 5. Suggestions — making the system more ACCURATE
1. **Gates currently CATCH, not PREVENT.** Exit 10/11/18 are post-render backstops. Each failure should drive a UNIVERSAL upstream fix + a regression invariant (CLAUDE.md rule #11) so iter-N catches iter-(N+1). Recurring families: BoM macro→sub-total propagation (gate 10 B-2), cover≠Σ reconciliation (B-3), `wrap={false}` layout smear (gate 11), narrative-vs-narrative numeric drift (gate 18).
2. **Graduate the self-correcting gates from SHADOW → ENFORCING.** Gates 31 (semantic self-audit), 32 (cost-sanity), 33 (physics-critic + auto-correct), 34 (tool-archetype coherence) are shadow by default. The physics-critic AUTO-CORRECT (fix the failing part, not just flag — `physics-critic-autocorrect.ts`) is the model: extend "fix, don't flag" to the BoM/numeric gates too. Enforce once stable per archetype.
3. **The semantic self-audit (gate 31, universal LLM-judge) is the per-section ≥8 accuracy meter** — class-agnostic, works on unseen archetypes. Use its per-section scores as the accuracy KPI; enforce on DECEPTION (blank headline / false-PASS compliance / £0 BoM) which it already detects deterministically.
4. **Independent cost-sanity (gate 32) is the magnitude net** — catches the £3,233/kW wind + £150/(t·yr) CO₂ misses by output-family £/unit bands. Widen `INDUSTRY_COST_BANDS`/`CLASS_OUTPUT_BANDS` as new archetypes appear. (Tristan feedback: ALWAYS independently sanity-check headline £/output-unit vs industry before calling a dossier good.)
5. **Expand verification beyond parts.** Parts existence (gate 20, distributor cascade) + per-line price (gate 21) are solid. Specs are checked only against a CURATED `KNOWN_PART_AUTHORITATIVE` table — add datasheet-grounded spec verification + writeback so spec claims are checked universally.
6. **Tag inferred vs verified everywhere.** The contact research uses verified/pattern-inferred/enquiry-fallback provenance — apply the same to engine-generated specs/MPNs so accuracy risk is visible, not hidden.

---

## 6. Gotchas the next terminal MUST know
- **TWO repos drift.** Use this one (newest). `git cat-file -e` lies about "is it merged" — use `merge-base --is-ancestor` or line-count diff. (MemPalace drawer `forgeos_gotchas_09cc653866b523bc`.)
- **`grep` treats `render-minimal-pdf.tsx` as binary** → returns nothing/exit 1. Use `grep -a` (or `git diff`).
- **Cost: the Agent tool runs ONLY Claude models** → spawning sub-agents for "cheap research" burns Claude credits. For cheap web-grounded research use **Gemini Flash via OpenRouter** (`google/gemini-3.5-flash:online`) from a script (template: `scripts/supplier-contacts.mjs`). Gemini Flash was also HIGHER quality for contacts than Sonnet. (Drawer `forgeos_gotchas_6db92c7cbd8f5577`.)
- **OpenRouter credits run out mid-batch** → 402. Balance: `curl -s https://openrouter.ai/api/v1/credits -H "Authorization: Bearer $OPENROUTER_API_KEY"`. Top-up = Tristan (dashboard). A 402 mid-chain truncates LLM JSON → empty advisorEngagement (looks like a content bug, is credits).
- **macOS TCC blocks the shell from `~/Downloads`** (ls/read EPERM). Keep working copies in the repo. The Write/harness file API can still write there.
- **Chain invocation:** `npx tsx scripts/serial-design-chain-v2.tsx <brief.md> <out-dir>` (defaults; no special flags). Loads `.env.local` from cwd → run from repo root. `next dev --webpack`.
- Roster harvest is idempotent per archetype; re-running overwrites cleanly.

---

## 7. Immediate next actions (pick one)
1. **#43 engine-quality** — fix the 5 gate failures universally (highest leverage for ≥8-all-archetypes). Start: `out/rerun-energy_storage/chain.log` + `chain-v2.pdf.AUDIT-BOM.md` (exit 10).
2. **#41 repo consolidation** — merge the two engines to one (kills drift). Decide merge direction first.
3. **Supabase push** — bridge forge-truth.db roster tables → app (`jyarhvinengfyrwgtskq`).

Tristan's standing aim (hold centre of mind): a UNIVERSAL engine that scores **≥8 on EVERY section for ANY archetype**, self-learning + self-correcting. The 3/8-clean re-run is the honest current distance from that.
