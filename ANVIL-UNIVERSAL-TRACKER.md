# ANVIL UNIVERSAL TRACKER — single source of truth

> Loop doc: `ANVIL-UNIVERSAL-LOOP-PLAN.md` (v2, council-remediated). Procedure: GENERATE → READ → REVIEW → IDENTIFY → FIX per iteration.
> Autonomous-turn-start rule applies: `cat ~/.engine-progress` then re-read THIS file.
> Repo: `CentaurOS-oxccu-efuel` only (two-repo drift #41 — do not edit the main repo).

## ANCHORS (diff-check after every revision)
- A1: ≥8 floor EVERY section ANY archetype, zero hand-holding.
- A2: Per-class-FAMILY sizing plug-ins + LLM-authored modules for uncovered classes (wall-3 verdict). No re-opening generic-structure; no MDAO rewrite.
- A3: Staged writeback promotion candidate→shadow→approved with rollback (G1). LLM judges advisory, deterministic checks decide (G2). Explicit convergence-failure fallback + honest labelling (G3).
- A4: Blender = structurally faithful schematic from contract data + validated i2i finish. Deterministic geometry checks (count/bbox±5%/collision/connectivity/CoG) gate.
- A5: Every fix universal + named regression invariant.
- A6: Frozen hidden holdout set untouched by learning until acceptance (G5).

## SCOREBOARD (update after every GENERATE)
| Line | Iter-0 (2026-06-10 rerun) | Latest |
|---|---|---|
| co2_mineralisation | exit 0 ✓ | |
| e_fuel_synthesis | exit 0 ✓ | |
| haps (registered brief) | exit 0 ✓ | |
| compute_heat_module | exit 11 ✗ layout | |
| energy_storage (BESS) | exit 10 ✗ BoM | |
| vertical_farm | exit 10 ✗ BoM | |
| satellite_smallsat | exit 10 ✗ BoM | |
| edge_ai_server | exit 18 ✗ drift | |
| HOLDOUT tidal_kite_generator | not yet run | run1: exit 10 misclassified wind. run2: W0 fired but exit 7, no graph. **run3 (w0b): FULL UNIVERSAL PATH FIRED** — W0 contradiction → minted slug tidal_kite_subsea_generator → auto-plan 25 tools → E6a bootstrap graph 10n/12e (G1 candidate) → generic emitter 10 modules/50 words/22 tools → skeleton critic correctly flagged the WIND-baked contract (coh 2/10). Run killed mid-flight at step 7.5 by parent TaskStop (no state written) — but next wall already proven: see item 23 |
| HOLDOUT biogas_digester_chp | not yet run | |
| HOLDOUT mushroom_farm_automated | not yet run | |
| HAPS-cold (payload-led brief) | known exit 7 (envelope) | |

## ⭐ TRISTAN LIVE CONCERNS — 2026-06-14 (PRIORITY; ⟳ = raised before + still open, he flagged this)

Raised in one message after the render colour/lighting fix landed (commit 581f5b02e, universal render now wears the bespoke studio lighting + module/service colour-coding). Tristan: "make sure these concerns go into the tracker as I have raised some of them before and they have not been fixed yet."

- **C1 — TOOL PLAN must self-generate ON THE FLY then register for reuse (the growing-DB pattern).** Tristan's decision on the wrong-tool bug (RAS got airfoil/AUV/bicycle physics): "the auto planner should be able to create new types on the fly. there is no fundamental difference between you creating code for a bespoke ras dossier and creating a ras dossier on the fly and then registering it so it can be used again. it should be the same concept that we use for the databases. look at database first … use that if available or then search and create a new category, add to database and then pull from database." → DB-first class-tool-plan lookup → on-miss GENERATE a domain-appropriate plan (LLM+corpus, same way a human would hand-wire it) → VERIFY → write back to a class_tool_plan store (class-tagged) → pull from store next time. **This EXTENDS the existing E6a bootstrap-on-miss (item #22, already does class-GRAPHS + B2/B3 does Blender-SCENES) to the TOOL PLAN (item #11 E4, still blocked).** ROOT of the wrong-tools: aquaculture_ras is half-registered (classifier+envelope+contract, NO tool plan/emitter) → auto-planner picks by loose output-key keysMatch with no working domain gate (applicability hook inert in fallback path; domain tags too coarse/mis-tagged — auv-hydro='thermal', bicycle='mechanical'). Refs drawer d2be3b3ce8d9958f.

- **C2 ⟳ — Blender: equipment OVERLAPPING (a big tank sitting ON TOP OF another fish tank).** The `place_all` core layout packs items into the same footprint. RAISED BEFORE ("how can all this equipment be sitting on top of the tanks"). Still open. High-risk file (drives CO2/SAF too) — fix the tank-grid footprint reservation so no vessel lands on another.

- **C3 ⟳ — Blender: pipe runs still don't look correct.** Routing legibility/correctness. RAISED BEFORE. Colour-coding now applied (water=blue/elec=red) but the ROUTES themselves still read wrong.

- **C4 ⟳ — 8 engineering drawings: the P&ID / pipe system is STILL not showing all 10 tanks.** RAISED BEFORE ("where are all the 10 tanks"). The drawing shows 1 rearing tank not the 10-tank array. Dense-topology / drawing gap.

- **C5 — Module/sub-module NARRATIVE may be the OLD word-engine, not the new physics/Blender/BoM.** Tristan: "I suspect that the document is using the old engine of words and it is not basing it off the new engine and blender loop plus new bom … this needs checking." **CHECKED 2026-06-14: suspicion CONFIRMED (high confidence).** The narrative is `buildNaturalLanguageLayer` from `src/lib/pdf-engine-v2/radical/sentence-generator` (chain :101) — the old "radical" word-engine (`overview_paragraph_en`/`module_brief`/RAD-syntax + LLM prose), a SEPARATE system from the new physics contract + Blender layout + requirements/component BoM. It splices SOME real numbers but is not driven by the new engine. NEEDS: a coherence pass so the prose is generated FROM (or reconciled against) the physics-derived equipment + Blender placement + new BoM, not an independent word tree.

## MISSING-ONLY RECAP (priority order — pick top ❌ when drift_flag=yes)
| # | Item | Increment | Status |
|---|---|---|---|
| 1 | ✅ exit-10 FIXED — ONE shared defect: Master BoM "Major Assemblies" section double-counted unmatchedMacros already injected in-module by computeBomTotals (B-3 cover≠Σ); removed duplicate section; invariants UNIVERSAL.master_bom_headers_reconcile_with_cover + bom_b3 parser false-fail fix; commits ab6a084e5 (mixed) + e9491a3d8; all 5 audited archetypes exit 0 | E5 | done 2026-06-10 |
| 2 | ✅ exit-11 layout FIXED — advisor header panel atomic (wrap=false) + reserve 16→96pt; invariant UNIVERSAL.advisor_header_panel_unbreakable; commit ab6a084e5; compute_heat exit 0, clean archetypes still 0 | E5 | done 2026-06-10 |
| 3 | ✅ exit-18 FIXED — brief-refinement oscillation (3.7→0.4→3.7 kW) left stale per-leg infeasibility flag; cover banner quoted it vs parsedBrief elsewhere. Deterministic net-out helper brief-infeasibility-net.ts at the banner consumer; invariant UNIVERSAL.brief_infeasibility_flag_nets_oscillation; commit 2717f5221; edge_ai exit 0, clean archetypes 0. Chain-side flag writer untouched (foreign-dirty file) — adopt helper there when chain file frees up | E5 | done 2026-06-10 |
| 4 | ✅ E0 DONE — harvest-tool-io.ts now reproducible (was pointing at the OTHER repo + writing /tmp); manifest 178→199 tools, empty output_keys 84→15 (irreducible dangling ids), planner-eligible 86→176; composeToolGraph novel-class 6→24 tools/55 edges, 3/3 universal outputs met; invariant UNIVERSAL.tool_io_manifest_backfilled; commit ee07e6105 | E0 | done 2026-06-10 |
| 5 | ✅ E1 DONE — envelope-vector.ts (typed vector, documented total-order primary-metric rule, 7 negative-extraction rules) + scoped a→b cascade in envelope.ts (strict detectors preserved verbatim); HAPS-cold exit-7 DEAD (generic tier sync, wingspan 40m inferred @0.85 conf via tier-c, $0.03); 50kW-vs-10MW + analogue traps pass deterministically; 8-brief byte-identical no-regression proof vs ee07e6105 baseline; 43/43 tests; commit 8df6d96a1. Chain adoption + harness port pending (foreign-dirty chain file) | E1 | done 2026-06-10 |
| 6 | ✅ B1 DONE — 16 parametric primitives (mm-typed, PRIMITIVE_API_VERSION 1.0.0) + placement relations + forge_scene_checks.py (6 deterministic checks, JSON output) + self-test (6/6 pass, exit-2 negative path verified); test render VISUALLY CONFIRMED as real aircraft + vessel skid by main session; bess-9shot regression clean; commit e6b77c5f0. Deferred: oriented-box collision, harness wiring (file was dirty) | B1/B3 | done 2026-06-10 |
| 7 | ✅ E2 DONE — sizing-families/ registry (typed plugins, score-threshold appliesTo, topo-sorted composition over shared namespace, WRITE_CONFLICT rule, G6 loud boundary errors, resize() grounding hook exposed); BATTERY ported byte-identical (legacy kept as oracle); process-plant 18 rules + aero-platforms 16 rules + coupled HAPS budget (wing 26.15 m², cruise 1324 W — plausible ranges); biogas composition test passes; wired into generic-emitter.ts; commit 64088801d. Left: 3 families (thermal/power-electronics/structures), grounding-loop wiring, envelope-vector type unification | E2 | done 2026-06-10 |
| 8 | 🟡 Holdout briefs DONE (briefs-holdout/: tidal-kite, biogas-chp, mushroom-farm + 3 SEALED hidden + haps-cold-payload-led; market-anchored ceilings in HTML comments; commit f69e19fc). Baselines NOT run — blocked by credits decision | loop | briefs done 2026-06-10 |
| 9 | ❌ E3 authored-sizing loop + G1 promotion store | E3 | blocked by 4,7 |
| 10 | ✅ B2+B3 COMMITTED 381536b52 — `scripts/author-blender-scene.tsx`: author → static validate (AST allowlist + kwarg schema, `validate_authored_scene.py` + `primitive-api-schema.json`) → deterministic Blender verify (`run_authored_scene_checks.py`, render stubbed in check mode) → revise ≤4 iters (+ fetch timeout — the prior stall was the unbounded OpenRouter wait — and `--resume` replay of failed iters from the out-dir) → G1 candidate row in forge-truth.db `authored_scene_candidates` (status 'candidate', NO auto-promote) → G3 `author-failure.json` + exit 5 on non-convergence. Prompt grammar = `PRIMITIVE-API.md` GENERATED from lib docstrings (`gen_primitive_api_doc.py` — rerun on lib change). Vision judge SKIPPED this increment (G2: deterministic checks decide; advisory judge is a later add). **E2E CONVERGED on tidal-kite holdout** (`test-fixtures/tidal-kite-state.json`): 4/4 iters (23→4→1→0 check fails), render-mode 6/6 pass, hero + per-module PNGs visually verified (gravity-base anchor + taut tether + winged kite w/ shrouded rotor — reads as the system, not boxes; kite is small in whole-scene frames because the 92 m tether dominates the envelope — hero-framing polish is an advisory-judge follow-up), candidate `tidal_kite_generator` v1 in DB. Harness invariant `UNIVERSAL.authored_scene_digest_spec_and_allowlist` PASSES (198/202 on oxccu-saf-v9; 4 FAILs pre-existing/unrelated). **CHAIN WIRING PENDING**: call `authorBlenderSceneForRegistryMiss(statePath, outDir, {resume?})` when generate-blender-scene exits 5 — serial-design-chain-v2.tsx still dirty in another workstream; wire in a follow-up increment | B2/B3 | done 2026-06-10 |
| 11 | ❌ E4 auto-planner population (mapper + conditional outputs) | E4 | blocked by 4 |
| 12 | ❌ E6 writebacks under G1 + snapshot-at-build | E6 | blocked by 9 |
| 23 | ❌ **W0b — ITER-3 KEYSTONE: class-fit check must fire AT CLASSIFICATION TIME, not at Stage-4.** Evidence (w0b run): U5 augmentation + engineering contract + macro-assemblies are built UPSTREAM of the orchestrator from the classifier's wrong class — a £658k WIND contract (45m rotor, 77t tower, 10 wind macros that can't land in the BoM) was baked into state before W0 fired; skeleton critic caught it (coh 2/10), identity-lock correctly blocked reviewers re-adding wind macro words. Fix location = serial-design-chain-v2.tsx classification step (:84 area) → **BLOCKED on resolving the foreign-dirty chain file** (that workstream must commit/land first). Secondary finding: generic emitter emitted air-cooling words (Cooling Fan/Air Damper) for a 40m-depth device — marine awareness belongs in the marine sizing family + skeleton derivation |  W0/E5 | queued — THE iter-3 blocker |
| 22 | ✅ **E6a DONE — bootstrap-on-miss VERIFIED IN-RUN** (w0b log): corpus-first + gemini-3.5-flash harvest → deterministic validation → class_graph_candidates G1 store (parameterised inserts) → emitter consumed bootstrap-candidate@v1 (10 nodes/12 edges, reused on second run, NOT promoted). + W0 slug minting verified ("tidal_kite_subsea_generator", contradicted label NOT kept). Commits ee1bdcc00 + ddba800c3 (next wall after W0; evidence out/holdout-tidal-kite-w0-chain.log: generic emitter throws "no class-reference graph for class" + "rely on bootstrap-on-miss" names a mechanism that doesn't fire). Build the Stage-0-harvest-pattern bootstrap: novel class → LLM+corpus harvest → verify → graph candidate (G1) → emitter reads it. PLUS W0 refinement: contradiction must mint a provisional novel slug from the brief (deterministic), not keep the contradicted class label | E6/W0 | in progress (sub-agent) |
| 13 | ❌ Repo consolidation #41 (decide direction, merge) | risk | queued |
| 14 | ❌ Calibration set: ≥5 human-scored reference dossiers for critic/self-audit | loop | queued |
| 15 | ❌ Reconcile harness I2.pages threshold (92>80) with accepted page growth from atomic advisor panels (ab6a084e5) — threshold update or layout tightening, decide deliberately | E5 | queued |
| 16 | ❌ Full all-archetype rerun to confirm 8/8 scoreboard in situ (all 5 fixes are render-path-verified; rerun = formality + catches interaction effects) | loop | blocked by credits decision |
| 19 | ✅ Circular import FIXED — unit-families.ts leaf module (12 family constants + findScaleMetric moved verbatim, re-exported from original site, zero consumer changes); dual-order import reproducer ok; E1 37/37 + E2 byte-identity re-verified; commits 2ee920c23 + c175aad4d (partial units unification) | E1 | done 2026-06-10 |
| 21 | ✅ W0 FIXED — class-fit-check.ts (CLASS_DOMAIN table from envelope DETECTORS, mobility/medium + output-family + negative-token signals, conservative bias documented) wired as orchestrate.ts step 1b: contradiction → loud log + G4 class_fit provenance + downgrade to genericEnvelope → universal path fires. Classifier confidence signal confirmed USELESS for novel archetypes (HIGH on keyword match). Tests 14/14: tidal-kite-vs-wind CONTRADICTION ✓, 8 registered briefs zero false positives ✓, biogas/mushroom honestly NOT caught (same-domain, future signal). Commit 4b945705d. CAVEAT: chain :84 classifier override foreign-dirty — class LABEL surfaces (blender dispatch) unfixed until chain adoption; in-situ re-run launched | W0 | done 2026-06-10 |
| 20 | ❌ DESIGN DECISION: real EnvelopeVector has no `domains` field but sizing plugins' appliesTo() reads it for the 0.75 domain-signal score — decide where domain signal lives on the vector, then complete the EnvelopeVectorLike→EnvelopeVector type unification (deferred from c175aad4d) | E1/E2 | queued |
| 18 | ✅ SECURITY (MED, FIXED 044282d38): author-blender-scene.tsx candidate-store INSERT now uses better-sqlite3 parameter binding (file CONTENTS bound, no readfile(), COALESCE(MAX(version))+1 kept) + assertCandidateStoreInputs() boundary validation (classSlug /^[a-z0-9_]{1,64}$/, apiVersion /^\d+\.\d+\.\d+$/, absolute quote-free paths, structured throw). Verified on throwaway DB copy: normal slug versions 1→2; malicious slug `x'; DROP TABLE…;--` rejected, table intact; real DB untouched | B3 | done (security review 2026-06-10) |
| 17 | ❌ Fix 15 dangling class-plan tool_ids (id typos: control-systems:run vs :pid-tuning, cell-balance-model:bms vs cell-balance:model, hvac-load:sizing vs hvac:load-sizing, …) — those plan steps silently never invoke their tools; changes registered-class runtime behaviour → needs verification runs | E4 | queued (found by E0) |

## BLOCKERS (need Tristan)
- OpenRouter balance $85.40 (checked 2026-06-10). Sufficient for fix-verification single-archetype runs; NOT for repeated full 12-line GENERATE sweeps (est. £3-8/run × 12). Top-up before iteration-2 GENERATE.

## ITERATION LOG
- **Iter 0 (2026-06-10, prior session):** all-8 rerun on latest engine = 3/8 clean. Artifacts: `out/rerun-<arch>/` (state.json, chain-v2.pdf, chain.log, AUDIT mds).
- **Iter 1 (2026-06-10, this session):** plan v2 written + 6-seat design council (2 BLOCK / 4 REVISE → remediations folded in). FIX phase started on items 1-2; READ/REVIEW evidence = iter-0 artifacts (fresh today, no regen needed).
- **Blender UNIVERSAL FLOW-DERIVATION (2026-06-11):** `scripts/blender-universal/build_universal_scene.py` — derive the DETAILED per-unit connectivity the sparse explicit topology omits (BESS shipped ONE bus edge, VF shipped no water loop). New `derive_flows()` (+ `_bop_anchors_to_families`, `_mechanisms_present_in`, `_has_role_part`) classifies each part's CONNECTIVITY ROLE (HUB / CONSUMER / COLLECTOR) by ROLE+mechanism — NEVER by archetype — and derives: hub → EACH consumer (supply fan-out) + each consumer → COLLECTOR → back to the source hub (closed return loop), routed on the SAME clean RackPlan/route_on_spine engine. Consumers = the rack anchors the placer emitted (15 BESS racks / 8 VF trolleys / edge-AI racks), NEVER leaf parts. Directional colours: fluid_supply (bright blue) vs fluid_return (teal); thermal (red) vs thermal_return (magenta). RICH topology (≥6 edges, e-fuel=8) DEFERS — only an electrical fan-out is ever derived. Wired into `_route_rack_farm_topology` (BESS/compute: drops the rack-aggregate explicit edge, adds DC-bus→each rack + rack_block→PCS→transformer chain + chiller→each rack→coolant-return→chiller) + `_route_panel_array_topology` (VF: panel→each rack + panel→HVAC, fertigation→each rack→return-drainage→fertigation). Fixed two real bugs: \b word-boundary regexes missed underscore-joined endpoints (led_array / lfp_cell_string) → (?<![a-z]) form; return-part picker mis-targeted HVAC condensate pump over the coolant return pipe → STRONG-prefer pattern. **VERIFIED (INSPECT renders /tmp/bl-univ-{vf,bess,edgeai,efuel}-deriv):** VF 28 routes (water loop CLOSES: 8 supply + 9 return; + 8 LED-power), BESS 51 routes (per-rack: 15 elec + PCS/transformer chain + 15 thermal supply + 16 thermal return), edge-AI 29 routes (was 0 — PDU→14 racks + CRAC→14 racks; air-cooled so no return, correct), e-fuel UNCHANGED (8 routes, identical audit). **ALL: over_equipment=0, same_elevation_crossings=0** (the hard gates); detour ~2 = honest spine routing (not spaghetti, over_routes=[]). Visually confirmed per-rack fan-out reads in top + iso + front views; supply/return are distinct lines. Unit tests `test_derive_flows.py` (30 checks, headless via stubbed bpy) PASS. Regression invariant: the test file guards per-rack fan-out counts + loop closure + rich-defer + the underscore-boundary + return-target-precedence fixes.
