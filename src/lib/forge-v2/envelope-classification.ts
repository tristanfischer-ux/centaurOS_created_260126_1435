/**
 * @file envelope-classification.ts — RETIREMENT STUB (2026-05-19).
 *
 * @description Original implementation archived during chain unification.
 * Container-class consistency checking is now done inside the chain via
 * scripts/serial-design-chain-v2.tsx + deployment-envelopes.ts. This stub
 * keeps the legacy /the-forge/cad-lab/parts-bom workbench compiling.
 */

export function checkBomContainerClassConsistency(..._args: unknown[]): { ok: true } {
    return { ok: true }
}

export function expectedContainerSizeToken(..._args: unknown[]): string | null {
    return null
}
