# Adversarial council — organoid-bioreactor-20260719-2150 (floor 9/10 "SHIPS")

**Verdict: the 9/10 SHIP is GOODHARTED.** Four independent adversarial critics (physics/domain, BoM/cost, PCB/firmware, scoring-honesty) each returned REJECT. The scores pass while the delivered design is plant-scale, carries wrong-domain physics, and has known-wrong content routed to advisory. Run this list down and fix at SOURCE (universal code), not per-instance.

## META-ROOT (fixes ~6 HIGH findings at once)
`benchtop_bioreactor` matches `PROCESS_PLANT_CLASS_TOKENS` (contains `'bioreactor','fermentation','fermenter'`) in `src/lib/pdf-engine-v2/lib/word-domain-coherence-audit.ts` → `isProcessPlantClass()` = TRUE. A device-scale override exists (`isDeviceScaleDesign`: true when `enclosure_volume_m3 < 1`; here 0.004 m³), **but only the coherence AUDIT consults it — the word-expansion, geometry, electrical, and cost stages key off the plant class token alone.** So a 20 ml bench device inherits plant templates everywhere.
**FIX:** a single `isProcessPlantScale(state) = isProcessPlantClass(class) && !isDeviceScaleDesign(state)` consulted by every stage that currently gates on the class token: word-expansion (H1), geometry emitter (H2), electrical model (M5), cost engine (BoM H2/H3), interconnect router (BoM H1).

## HIGH
- **H1 [physics/domain] cartridge_heater → pressure-filter plant template.** 9 process-plant-vessel words emitted from `cartridge_heater_word__*` (Pressure Vessel Shell, Filter Media/Membrane, Distribution Header, Underdrain/Nozzle Plate, Backwash/Service Valve Nest, DP Gauges, Air Scour, Sample Cock, Skid Frame & Pipework). The word "cartridge" collided with cartridge-*filter*. SOURCE: word-expansion/radical selection for `cartridge_heater` in a device-scale class.
- **H2 [physics] metre-scale part geometry.** Culture Vessel `1544.8 dia × 1600 mm = 3.106 m³` (770× the 0.004 m³ enclosure); Magnetic Stirrer 1.277 m³; Vial Holder 1800 mm. Electronic parts ARE correctly device-scale — so the fault is confined to fluid/thermal/structure parts inheriting plant default envelopes. No invariant checks Σ part-volume ≤ enclosure. SOURCE: geometry emitter plant defaults.
- **H3 [BoM] £2,005 DN25 industrial pipe interconnect** (2.4–5.1 m runs, `via: pipe DN25`), EXCLUDED from the headline so invisible. Should be cm of ~3 mm silicone micro-tubing. SOURCE: `fluid_loop` interconnect router defaulting DN25 when flow/bore unknown.
- **H4 [BoM/cost] £259 = masked ×0.461 gold-band rescale of a £541 plant build.** Class-reference medians: Culture Vessel £20k, Vial Holder £42.5k, actuation assembly £5.38M. Every line "0.0× the class median." SOURCE: `class_reference` curve is process-plant; blanket rescale Goodharts the target band instead of pricing real parts.
- **H5 [physics/scoring] wrong-archetype 3D-PRINTER tools ran + marked USED.** `extruder:thermal → 210.48 °C, 46.2 W`; `motion-kinematics:steps-per-mm → 80 steps/mm`. Surfaced as contract quantities (`extruder_power_w`, `printer_steps_per_mm`) + rendered as the bioreactor's live "Calculations". Gate 34 (tool-archetype-coherence) has only marine/hydroponic/refrigeration/irrigation markers — NO additive-manufacturing family → walks through. SOURCE: relevance-sweep admits printer tools; gate 34 marker gap.
- **H6 [PCB] firmware/software proof NOT wired.** `deriveFirmwareProofSpecs` (`pcb-firmware-proof-spec.ts`) + `prototypes/pcb-firmware-proof/firmware_proof.py` exist but are DEAD (called only by their tests). No compile/pin-contract/bus-address/channel-count proof runs. SOURCE: never invoked by `pcb-stage.ts`.
- **H7 [PCB] multi-board under-coverage.** `architecture.systemDisposition = multi_board`, 3 boards declared (`wet_lab_hat`, `od_optics`, `wet_actuation`), each `requiresKiCadDeliverable`. Only ONE built; `od_optics` + stir + pump driver channels absent (0 of 4 required channels implemented). Coverage gate passes (16/15=107%) because the denominator is the flat `electronicPartCount` (already shrunk to placed parts), not `Σ architecture.boards[].requiredWordIds + Σ channelRequirements`. SOURCE: `build-excel-export.py` `_pcb_scope_and_fitness` denominator + `pcb-stage.ts` builds one board.
- **H8 [PCB/scoring] FAB-READY doesn't disclose UNPROVEN-IN-HARDWARE.** No firmware proof + no HIL, yet an unqualified green "FAB-READY 9.7". Doctrine: max verdict without firmware proof = "FAB-READY — UNPROVEN IN HARDWARE". `state.pcbGate` fired `clean_board` in SHADOW — hygiene-only. SOURCE: `_pcb_readiness_verdict` must cap the string + `pcb-gate.ts` widen fire condition.
- **H9 [scoring] stability HARD brief metric false-PASS.** Brief: "37 °C with stated closed-loop STABILITY (the design MUST hold and report this)". Dossier proves only the setpoint echo (37==37); NO ±°C band / settling / ripple anywhere. Requirement-echo trap. SOURCE: verification spine + E6 matcher accept setpoint echo, not a derived `temp_stability_c`.
- **H10 [scoring/BoM] 35 of 37 BoM lines "bespoke fabrication to drawing"** incl. MCU, USB, Peltier, cartridge heater, fan, temp sensor — all CATALOGUE parts the brief mandates. Part-names scores 10/10 because the check counts a non-empty string. SOURCE: MPN-coverage scorer must reject "bespoke fabrication" for electronic/catalogue families → count UNRESOLVED.

## MED
- **M1 electrical plant model on a 35 W USB device:** 400 V 3-ph, 25 kVA incomer, 16 A breaker, DN15–DN300, steam 30 m/s, HVAC ducts, "runs 8000 h/yr". SOURCE: electrical/connection-sizing plant constants un-gated on device scale.
- **M2 redundant Peltier TEC + cartridge heater** on a ~1 W thermal duty. SOURCE: `peltier_tec_module` sub-module emits both.
- **M3 pump MPN is silicone tubing** (Watson-Marlow TUB-SAN-6.4 = Pumpsil tubing, not a pump), marked `confidence: high` on an admittedly-unfound part. SOURCE: slot-mispin + part-verification emits confidence:high on no-match.
- **M4 cost waterfall = Lang/Hand/CEPCI plant factors + EPC/skid** on a lab kit. SOURCE: cost-waterfall shares plant install-factor library across classes.
- **M5 BoM↔PCB contradiction:** PCB gives MCU real MPN (ATSAMD21G18A-AU); BoM calls same MCU "bespoke fabrication / FR4". SOURCE: no BoM↔PCB identity reconciliation.
- **M6 Engineering Analysis tab EMPTY** (0 parts analysed) but scores 10/10 on ASME BPVC/ISO 2768 boilerplate rows. SOURCE: `_sc_engineering_analysis` accepts SF-input/reference rows as "analysis".
- **M7 heater mis-pin:** PCB R1 "Cartridge Heater" = Rohm ESR18EZPJ3R9 (a 3.9 Ω 1206 chip resistor) pinned as the culture heater.
- **M8 dosing-pump torque 256 mN·m** implausible for a 20 ml micro-dosing pump at 8.3 RPM.

## LOW / enforcement
- **Enforcement gap (systemic, highest-severity per physics critic):** the engine DETECTED most of this — `wordDomainCoherence.verdict=flagged` (9 words), physics critic scored 7/10, volume error caught — but ALL routed to `advisory:true / never scores`; `gatesPassed:false` overridden by `acceptanceStatus: accepted_with_decisions`. Detection exists, enforcement does not (Gate-Intent failure).
- **SIGHT is shallow:** render/drawing vision critics run only a broken/blank checklist ("is it broken"), never function-driven morphology ("is this a credible bioreactor; optical axis through vessel; no plant drawing notes"). GA = two grey domes; exterior = open-top vessel on a "spill-contained" instrument. SOURCE: extend vision checklist with class morphology probes + drop ASME/corrosion drawing boilerplate for instrument-class.
- Plant-scale ⚠Checks invariants pass vacuously (16 mm² cable, mains incomer breaker) — gate behind `isInstrumentDevice`.
- Sanity band £100–£5,000,000 (a £5M benchtop kit passes); E4 price check circular (vs engine estimate); connections £2,005 checked by no invariant.

## Fix order (leverage)
1. **Meta-root:** `isProcessPlantScale(state)` gates every plant stage (word-expansion, geometry, electrical, cost, interconnect) → kills H1/H2/H3/H4/M1/M4.
2. **Enforcement:** flip word-domain-coherence + physics-impossible-geometry (Σ part vol > enclosure) from advisory to SCORING/BLOCK for device-scale.
3. **Gate 34:** add additive-manufacturing/extruder/printer marker family (+ generic tool-domain ∉ class whitelist) → kills H5.
4. **PCB software:** wire `deriveFirmwareProofSpecs` + `firmware_proof.py` Tier-0 into `pcb-stage.ts` (per architecture board); cap FAB-READY→"UNPROVEN IN HARDWARE" without proof; fix coverage denominator to architecture scope + demote un-built boards → kills H6/H7/H8.
5. **Scoring honesty:** stability metric (H9), catalogue-part rejection of "bespoke fabrication" (H10), Engineering Analysis non-empty (M6), BoM↔PCB reconcile (M5), confidence-honesty on unfound MPN (M3).
6. Each fix carries a proveCatch keyed on THIS run's state (Core Fix Principle).
