# BACKLOG — the single durable ledger of agreed work (anti-"say-it-then-never-do")

> **THE RULE (closes the capture gap):** every idea agreed with Tristan is appended HERE in the SAME turn it is discussed — never "later". If it is not in this file by the end of the turn it was raised, it does not exist.
> **DONE ≠ captured.** An item is `✓DONE` only with a proof artefact: a commit SHA **and** a full-run number (scorecard / verify output). No proof = not done.
> **Audit in 10 seconds:** `bash scripts/verify-floor.sh` (the scores) + this file (the list). Don't trust me — read this file.
> **Forcing function:** at the start of any work session, read this file and work the highest-priority non-DONE item. Do not generate-new-and-forget. Do not end a session with undone items unlogged.
> Status key: 💡 idea · ✅ agreed · 🔨 in-progress · ✓DONE(proof) · ⏸ parked
>
> **STANDING CONSTRAINTS:** (1) **NO PDFs** — we are not reviewing PDFs; the **Excel `dossier.xlsx` is the review surface**. (2) Every tweak UNIVERSAL (no `if ras`). (3) Verification must be DETERMINISTIC arithmetic, not LLM (§B). (4) **No Gemini i2i** — no photoreal hero, no module images; keep only the Blender geometric renders.
>
> **EXCEL-GOAL RUN CONFIG (use on every chain run):** `CHAIN_SKIP_RENDER=1` (no PDF) · `CHAIN_SKIP_IMAGE_GEN=1` (no Gemini hero + no Gemini module images; Blender renders kept) · `QUALITY_LOOP_MAX_ITERS=1` · `MAX_CODE_FIX_CYCLES=0`. (Also skip supplier-enrichment for verify-only runs.)

## ▶ RESUME HERE (post-compact, 2026-06-20 ~01:30) — Blender pipe fix + £5M-exact
1. **IN FLIGHT:** £5M-EXACT re-run at **55 t/yr** → `out/ras-5m` (bg `blo3tbz0o`; brief `briefs-rerun/yellowtail-kingfish-ras-5m.md` now @ 55t, **UNCOMMITTED**). WHEN DONE: check `state.costStack.installed_asp_gbp ≈ £5.0M` (45t was £4.42M; six-tenths → 55t ≈ £5.0M). If off >~1%, nudge tonnage in the brief + re-run. Then confirm deterministic `scripts/deterministic-checks.py out/ras-5m` 0-fail + floor 9, regen `build-excel-export.py out/ras-5m` (modules already removed, #NAME already fixed), copy timestamped xlsx to ~/Downloads, **commit the brief + result**.
2. **NEXT (agreed w/ Tristan): PIPE FIX (D8) — do Step 1 FIRST.** Pipe cost £467k = 18% of BoM, ~5-8× too high (loose Blender placement → 3,783 m pipe; Tristan caught it visually). **Step 1 (low-risk, do first):** add a deterministic check `per-pipe-run ≤ building_diagonal × ~1.6` + `total pipe ≤ k·√footprint·n` (`deterministic_checks_lib.py` — closes the blind spot) + CAP the costed length to that plausible value (`requirements_bom.py`) → £467k → ~£100-150k. **Step 2 (visual, needs eyeball loop):** tighten placement in `scripts/blender-universal/build_universal_scene.py` (clamp `FLOW_TRAIN_SINGLE_LANE_MAX_MM=52000` toward the 27 m building width + cluster connected equipment via the connection graph). **DEPENDENCY: the pipe fix drops the BoM ~£350k → £5M then affords ~65 t/yr → RE-BUDGET £5M-exact AFTER the pipe fix.** Principle drawer `forgeos_decisions_417ca0248485359b`; D8 below.
3. HEAD `70b850a12` = origin/main, clean (bar the 55t brief). Pre-pipe-fix demo artefact: `~/Downloads/RAS_5M_dossier_2026-06-20_0027.xlsx` (45t). Done this session: live economics + investor sweet-spot model; swarm-fixed BoM (heater £49.8k, blower 2.5 kW, £0-valve→£1,800, per-tank 184 m³, cost-waterfall reconciled, dup heat-pump gone); module tabs removed; cost-waterfall #NAME fixed.

## A · FLOOR-8 (RAS, Monday demo) — every scorecard section ≥8
- A1 ✓DONE INC-0 scorecard de-dup + self-rewriting code-fix gated off — SHA `2475c38a6` (pushed)
- A2 ✓DONE INC-1 tool_archetype 2→10 — SHA `58439188a`, confirmed end-to-end in run ras-inc3 (pushed)
- A3 ✓DONE INC-3 physics — pump per-unit EXPLICIT 1,670 + main breaker 4,000 A + biofilter tank 153 + panel reconcile. **Verified run ras-inc4: floor 2→5, mean 8.5, no regression**; pump word now `rating_secondary=1,670 m³/h` (×8 = 13,360 ✓). SHA `a98db4d76` (pushed). The critic STOPPED hallucinating once the duty was explicit.
- A8 ✅ RESIDUAL (real, the new physics_fidelity=5 driver): pump PART-SELECTION — engine named "Grundfos CR 155-4-2" (catalogue max ~220 m³/h) for a 1,670 m³/h duty (~7.5× too small). Same class as Grundfos UP15-42 (D1). Fix = validate pump MPN catalogue max-flow ≥ duty at selection (reject undersized) + pick a real large pump (e.g. end-suction/split-case ~1,670 m³/h). Caught deterministically by the rating≥duty check (§B/agent building).
- A4 ✅ INC-2 brief_compliance — value+unit fallback mapping for the 5 "—" metrics. Proof: full-run brief_compliance ≥8.
- A5 ✓DONE INC-4 connectivity — deterministic connection-graph completion UNIVERSAL (electrical distribution spine Utility→Genset→Breaker→Busbar-as-hub + blower-output fix + identity-fold/dead-leg audit + 2 new CONNECTIVITY coverage checks). SHA `959a44972` (pushed). **Proof:** live ras-inc5 audit-only process 87.0% / instruments 100% (scorecard 6→9, clears ≥8); deterministic CLI **281/0**; Excel ⚠Checks **0 fail**. Byte-stable SAF (identical) / CO2 (+1 correct blower edge) / VF. Full lean re-run → out/ras-inc6 IN FLIGHT (refreshes schedule → process 91.3% / electrical 8/8 end-to-end).
- A6 ✅ INC-5 drawings read ONE ledger + hero reframe (fill the frame, drop the empty ground plane). Proof: eyeball + drawing_gates ≥8.
- A7 ✅ Fix drawing_gates regression — panel load_reconcile 2,865 vs contract 1,719 (connected-load scope). Proof: full-run drawing_gates ≥8.

## B · DETERMINISTIC VERIFICATION — the architecture reframe (Tristan 2026-06-19) ⭐
> Principle: a number is trustworthy only if you can WATCH it be computed; discrepancies flagged by deterministic ARITHMETIC, not an LLM. The Excel IS the verification engine. CAGE the LLM critic.
- B1 ✅ Every number is DERIVED (formula + visible inputs) or SOURCED (provenance link) — no bare assertions. Needs: expose inputs on the 25 legacy calcs + BoM price derivation.
- B2 ✅ Complete deterministic CHECK SUITE (Excel ⚠Checks + mirrored engine gates), all pure arithmetic: consistency (per-unit×count=total, Σsub=principal, emitter=contract, cross-page equal); adequacy (rating≥duty: pump / breaker / cable / tank>media / chiller); balances (mass / energy / flow); cost (per-line band, Σ=cover).
- B2 ✓DONE deterministic check suite built + GREEN (ras-inc5: **279 pass / 0 fail**, 0.1 s). CLI `scripts/deterministic-checks.py`, lib `deterministic_checks_lib.py`. SHAs 5c26398be (suite) + b55d47481 (9 engine fixes) — pushed.
- B3 ✓DONE(code; out/ras-inc6 run + typecheck verifying, commit pending) DEMOTE the LLM critic — `computeQualityScorecard` (serial-design-chain-v2.tsx): LLM self_audit sections now tagged `advisory:true`; **floor + allPass computed from the DETERMINISTIC sections only** (drawing_gates / cost_sanity / physics_gates / tool_archetype / connectivity); `extractFixDirectives` skips advisory so the loop never chases a critic misread. Universal fallback: a class with ONLY advisory sections still scores (no blank). LLM concerns stay in `sections` (visible on dashboard/Excel, non-gating). **Proof on REAL ras-inc5 data: OLD floor 6 (dragged by physics_fidelity=6 misread + brief_compliance=7 wobble) → NEW floor 8, allPass=true.** This is the architecture reframe: the deterministic checks ARE the quality bar; the LLM may never turn a green check red or set the floor alone.
- B4 ✅ Make numbers EXPLICIT on the words so the LLM can't misread (= the pump-1336 root cause; task #123).
- B5 ✓DONE Capex-ceiling breach now SURFACED deterministically. The engine recorded `closures[3] capex_within_ceiling status=warn` (£8.15M 204 t/yr design vs the £5.0M brief ceiling — the #1 customer constraint) but the suite DROPPED it (only `fail`/`error` closures surfaced, never `warn`) → CLI + Excel both falsely read all-green. FIX (universal): `deterministic_checks_lib` surfaces ANY closure the engine marked warn/fail/error as a FAIL (its own verdict, never fabricated; pass stays silent); `build-excel-export` renders a status-only FAIL as a static red ⚠Checks row (it previously skipped `expected=None` checks → Excel disagreed with the CLI). Now **CLI 300/1 == Excel ⚠Checks 1 fail == the capex breach, visible**. Selftest extended both directions. **⚠ This is VERIFICATION, not the fix — the design IS ~2.5× over the £5M ceiling. #97 design-to-budget is Tristan's call: scale to the £5M-achievable tonnage (~125 t/yr) OR present 204 t/yr as the full unit + report what £5M buys.**

## C · SPREADSHEET (dossier.xlsx) — the 50 improvements + provenance
> Full 50 + phased plan captured from the sub-agent deliverable (Phase 1 quick wins · Phase 2 bigger · Phase 3 engine). Sequence the VERIFICATION items (B2) to the FRONT.
- C1 ✅ Provenance: confidence tiers (green/amber/red), "Verified by" column (catalogue/distributor/parametric/LLM), surface the quantity tool_id/URL/uncertainty already in state.json, source-URL hyperlinks.
- C2 ✅ Schedules-as-tables: panel schedule → tab; process schedules → tab (line/valve/instrument); line&velocity tab (within_spec flag); parts-manifest + cable/pipe take-off tabs; SLD/P&ID/BFD companion tables beside the picture.
- C3 ✅ Nav/UX: Contents tab + hyperlinks + back-links; autofilter; number formats (£#,##0 / #,##0); print areas + headers; freeze panes; tab colours; grouping/outline.
- C4 ✅ Interactivity: single Inputs tab; scenario columns (low/central/high); sensitivity/tornado; data-validation dropdowns; named ranges.
- C5 ✅ Engineering value: spec-sheet-per-principal; cost waterfall; brief-compliance matrix; energy/OPEX; assumptions tab.
- C6 ✅ Sharing/accessibility: README/how-to-read; glossary; locked-vs-editable; metadata (run id/SHA/date/disclaimer); colour-blind-safe palette.
- C7 ✅ ENGINE changes (Phase 3): per-BoM-row tool_id provenance; structured `basis` fields on requirementsBom/costBasis.
- C8 ✅ Fix exporter bug — value-equality chaining grabs the wrong cell (wall & roof both = 4920 → both ref B30); chain by symbol+label, not value.
- C9 ✅ Wire the exporter into the chain — auto-generate dossier.xlsx on every run.
- C10 ✅ Exporter auto-TIMESTAMPS the filename (e.g. `RAS_dossier_<run>_<YYYY-MM-DD_HHMM>.xlsx`) so versions are trackable. (For now done by hand on copy.)
- C11 ✓ Check PRECISION fixed in the CLI: the new `deterministic_checks_lib.py` binds each per-unit to its own denominator — the 1,336×8 false-positive now correctly PASSes (per-tank 1,336 ÷10 vs per-pump 1,670 ÷8). CLI = the oracle (out/ras-inc4: 256 pass / 9 fail in 0.1 s).
- C12 ✓DONE EXPORTER aligned: ⚠Checks tab now sources ENTIRELY from `deterministic_checks_lib.py` (Excel == CLI, 279/0); stale 1,336 + motor-power checks removed; latent COST live-formula band bug fixed. SHA 59b5a9b5f (pushed).
- C-TIER2 ✓DONE (SHA e2352ac4b, pushed): Contents tab (#26) + back-links, number formats (#37), brief-compliance matrix (#45, all 5 PASS), cost waterfall (#44, £13.4M = ASP), spec-sheet-per-principal (#43), schedules-as-tables (#20–22: panel/process line/valve/instrument + line-velocity within_spec red). Excel 36 tabs, ⚠Checks 279/0, universal (co2 skip-path proven). Latest: `~/Downloads/RAS_dossier_ras-inc5_2026-06-19_1715.xlsx`.

## D · KNOWN DEFECTS (found this session)
- D1 ✅ Grundfos UP15-42 — a real domestic-circulator MPN marked "IDENTIFIED" and priced £67,900 as a 97 kW pump. Fix the part selection; the B2 part-vs-duty + price-band checks catch it.
- D2 ✅ The LLM physics critic mis-reads correct deterministic designs (read a per-tank 1,336 branch flow as the pump total) — the reason for B3/B4.
- D3 ✓DONE CWE-1236 Excel formula-injection (security-review finding) — a display string from state.json (LLM-derived) starting with `=`/`+`/`-`/`@` executes as a formula when the customer opens dossier.xlsx. `clean_cell` now defangs with a zero-width space; ALL un-cleaned external string writes routed through it (sub_banner, spec-sheet status/material, requirementsBom requirement/part/basis, costBasis method/notes, quantities name/unit/family/source). Live formulas (`cell.value="=…"`) bypass clean_cell → 58 live calcs + ⚠Checks unaffected. SHA `10297539c` (pushed). **Proof:** defang unit-test 8/8 (triggers escaped, text+numbers untouched); exporter 36 tabs/58 live calcs; CLI 281/0; Excel 0 fail.

- D4 ✓DONE(exporter display) Sub-component £unit values are absurd on `£line=0` assembly rows (ras-inc5 P-101 pump: ↳Drive Motor 110 kW £137,265, ↳VSD £184,740 — each EXCEEDS the £67,900 parent). Harmless to TOTALS (line=0, 281/0 Σ-checks pass) but a DEMO RISK on the Excel BoM. **Pump parent is correct** (D1 fixed: the wrong *named* Grundfos part is GONE → parametric "rating-based", £67,900×8 plausible for 8× seawater 97 kW pumps). **FIXED exporter-side:** a `↳` sub-component with `line_gbp==0` now renders "incl. in parent" / "—" instead of the parametric intermediate. Verified ras-inc5: P-101.1..13 read "incl. in parent", parent intact (£543,200), ⚠Checks 0 fail, 58 live calcs. SHA pending. **ROOT (post-Monday):** the decomposition in requirements_bom.py assigns the absurd sub-prices in the first place (motor £137k ≫ pump £67.9k) — fix the parametric allocation so sub-£unit ≤ parent, then they can be shown.
- D5 🔨 **3 REAL scaling defects the £5M (45 t/yr) re-run exposed** (deterministic CLI caught them — the suite working): (a) **per-tank flow 1,336 m³/h did NOT rescale** — it's the 204t per-tank value, now 1,336 > the 737 m³ total tank volume = IMPOSSIBLE → a quantity is pinned to the 204t scale instead of deriving from production (unit-family / non-scaling-literal bug class); (b) **aeration blower emits 3 kW vs contract 2.5 kW** (emitted-rating ≠ contract); (c) **Backup Immersion Heater cable 240 mm² < 630 A ampacity** = undersized feeder. ALL need UNIVERSAL fixes (would affect any resized class). The swarm audit + BoM deep-check will likely surface more.
- D6 🔨 **Capex-closure BASIS bug** — `capex_within_ceiling` compares the EQUIPMENT anchor (£1.80M on ras-5m) to the £5M ceiling, but the brief ceiling is on INSTALLED cost (£5.04M). So it PASSES while installed is £38k over. ALSO the anchor scales LINEARLY (1.80/8.15 = 0.22 = 45/204) while installed scales six-tenths — two different cost laws. FIX (universal): the closure must compare `costStack.installed_asp_gbp` to the installed-capex ceiling, and reconcile the anchor's scaling with installed.

## E · £5M MODEL + SCENARIOS + ECONOMICS + CHARTS + PLAUSIBLE VISUALS (agreed Tristan 2026-06-19, FishFrom demo) ⭐
> Tristan: "set the model INCLUDING blender at £5M capex, optimise for it, THEN scenarios up/down from there; economics with fish £/kg + input costs; lots of relevant charts everywhere; make the Blender image + other images as deterministically plausible as possible." → Move the WHOLE realised design (engine + Blender + 8 drawings) to **£5M as the centre point**; the Excel becomes a LIVE scenario + economics model around it. Architecture: live deterministic model (Excel, flexes) vs realised snapshot (engine/Blender/drawings, one scenario).
- E1 ✓DONE **£5M REALISED design — GREEN** (out/ras-5m; SHA 2130b81f6 + per-tank source fix). 45 t/yr, installed **£4.42M ≤ £5M**, scorecard **floor 9 / mean 9.8 / allPass**, deterministic **257/0**. After the swarm-fix set: heater £49.8k, blower 2.5 kW, £0 lines 0, ONE heat pump (P-103 £32k), per-tank **184 m³** (each×count==total), cost waterfall reconciles (£2.96M→£4.14M→£4.42M). Demo artefact `~/Downloads/RAS_5M_dossier_2026-06-20_0027.xlsx` (40 tabs incl. live economics + investor sweet-spot). The earlier £5.03M run had oversized 334 m³ tanks + a £155k duplicate heat pump — BOTH corrected by the per-tank source fix. £4.42M leaves ~£0.58M headroom (could fit ~50t for the full £5M — a design-to-budget refinement, not a defect; the Excel sweep shows it).
- E2 ✓DONE **Economics model (Excel, live)** — Inputs/Economics tabs, all live formulas off editable cited inputs (fish £22/kg, feed £2.1/kg, FCR 1.37, energy £0.15/kWh, labour, maint 3%). Replaces the `yield-economics` bootstrap stub. Verified ras-inc6: revenue £4.49M, EBITDA £1.64M, payback 7.6 yr, IRR 11.8%. Universal (co2 t/day proven).
- E3 ✓DONE **Scenarios + SWEET-SPOT finder** — 16-row log sweep 0.2×–5× output, live capex(six-tenths)/revenue/opex/EBITDA/payback/NPV(annuity)/IRR(`RATE`); Investment Analysis tab finds break-even / viability(IRR≥discount) / investable(IRR≥editable 15% hurdle) / NPV-max / £5M-anchor + recommended-deployment callout. **Headline: 45 t/yr ≈ break-even (IRR~0); 204 t/yr IRR 13%/7yr; investable threshold ~281 t/yr → at £22/kg even the full plant is below a 15% hurdle (needs higher price or more scale).** Universal.
- E4 ✓DONE **Charts** — opex pie, revenue/opex/EBITDA bar, capex-vs-output, payback-vs-output, IRR-vs-capex with discount+hurdle threshold lines, EBITDA-margin-vs-scale (openpyxl native).
- E2 ✅ **Economics model (Excel, live)** — Inputs tab (fish £/kg, feed £/kg + FCR, energy £/kWh, labour, maint %, density, turnovers, tonnage, capex) → revenue / opex / EBITDA / NPV / IRR / payback as LIVE formulas; grounded + CITED defaults; replaces the `yield-economics` bootstrap stub's hidden assumptions. New ECONOMICS ⚠Checks family (revenue/opex/payback gated).
- E3 ✅ **Scenarios** — flex ALL core inputs up/down from £5M; Low/Central/High + £5M columns; `capex = base × (t/204)^0.65` live; solve-for-tonnage from a capex ceiling. Data-validation + named ranges.
- E4 ✅ **Charts everywhere** — cost waterfall, capex-vs-tonnage curve, payback curve, opex breakdown, revenue-vs-opex, sensitivity/tornado, scenario comparison (openpyxl native charts).
- E5 ✅ **Visuals deterministically PLAUSIBLE at £5M** — Blender hero + 8 drawings regenerated at the £5M scale; drawings render ALL ~113 connections + instruments (density gate), legible; Blender plausible massing (NOT photoreal; Tristan-rejected before — correctness/plausibility over beauty).

- D7 ✓DONE Heat-pump P-101 DUPLICATE — RESOLVED by the per-tank source fix cascade: correcting tankVolumeEachM3 (334→184) corrected the heating-duty derivation, which removed the duplicate heat-pump emission. Verify re-run (out/ras-5m): ONE heat pump (P-103, £32,375, 83 kW), deterministic 257/0 (the Σsub==line fail gone), floor 9. [original diagnosis below kept for reference]
- D7-orig 🔨 Heat-pump P-101 DUPLICATE — the BoM has TWO "P-101 Heat Pump" lines: "88 kW" £33,532 (sub-components P-101.1..10 foot correctly) AND "289.7 kW heating" £188,305 (no sub-components). Same physical heat pump emitted under two rating VIEWS (electrical 88 kW vs thermal 289.7 kW @ COP~3.3), tag-colliding on P-101 → deterministic "Σsub == line" fails (33,532 vs 188,305). NEW (not in pre-fix ras-5m-prefix's 3 fails) — likely the equipment synthesis matches the heat pump under two quantity keys (power + heating-duty), exposed once the duty-scaled class budget priced the thermal view. FIX (universal): de-dup principal equipment resolving to the same physical unit under multiple rating quantities — emit ONE line keyed by part identity (thermal duty as the rating, electrical as a sub-spec). Investigate universal-contract-sizing.ts synthesis. Was NOT chased at 00:30 — capture-then-fix.

- D8 🔨🔴 **PIPE LENGTH→COST inflated by loose Blender placement (Tristan caught it visually 2026-06-20)** — `out/ras-5m`: total pipe **3,783 m**, runs up to **77 m**, connection/pipe cost **£467k = 18% of the £2.6M BoM** at £123/m. A 45 t/yr / 4-tank plant should be ~400-700 m / ~£60-100k → pipe is ~5-8× too high (~£370k phantom). MECHANISM (Tristan): Blender's layout sets the route length, the ledger costs it length×£/m — so loose placement inflates cost. ROOT: `FLOW_TRAIN_SINGLE_LANE_MAX_MM = 52000` (52 m single lane >> the 753 m²/~27 m building) strings equipment wider than the building; ancillaries (chilling/feed/bleed/skimmer) land far from the tanks they serve; overhead routing (up-along-down) ~doubles each planar run. The deterministic checks PASS it (length×rate is sound) — the blind spot is no check on whether the LENGTH is physical. FIX (universal): placement optimiser (#149) must pack CONNECTED equipment adjacently + clamp the train to the building footprint + route laterals within one bank; add a deterministic check "total pipe length ≤ k × building_diagonal × n_connections" (or per-run ≤ building diagonal) so an inflated layout trips. Re-render + re-cost; expect installed ~£3.9M (more headroom under £5M). build_universal_scene.py. Not chased at 01:00 — capture-then-fix-with-a-clear-head.

## F · SWARM AUDIT (2026-06-19) — 14 agents, 72 findings → 9 groups, triaged (real vs false-positive)
> Verdicts: BoM "customer right to be suspicious" (token-priced process systems, £0 valve, per-tank bug); visuals "NOT demo-ready" (P&ID 25%, 26 tags absent) but Blender hero is sound (85%). I critically triaged — several "bugs" were Haiku misreads.
- F-G1 Economics "4 formula bugs" — **MOSTLY FALSE POSITIVE (verified by reading the formulas).** The Low/Central/High block CORRECTLY builds worst/best corners (Low = low price + HIGH energy + HIGH capex, documented L2114-2117); the auditors misread the cross-column refs as swaps. Energy×(out/ref) in the sweep is CORRECT (energy scales with plant size). NPV-at-discount-rate is CORRECT (hurdle is for IRR only). GENUINE residue: add an EBITDA<0 red conditional-format flag + a break-even "not in range" guard (low-impact; break-even ~50t IS in the 9-225t sweep). DON'T apply the false-positive fixes.
- F-G2 ✓REAL BoM price floors missing for process systems (33 lines <£100, £0 DN400 valve ×4) → agent (requirements_bom.py). **Confirms Tristan's BoM suspicion.**
- F-G3 ✓REAL per-tank volume didn't rescale (334×4=1,336 vs 737) → agent (universal-contract-sizing.ts).
- F-G4 ✓REAL factory_cogs/oem_transfer exclude £1.19M field-install → agent (class-cost-structure.ts).
- F-G5 ✓REAL 4 hallucinated/wrong-type MPNs (pump series as a valve, MCU as a switch) → agent (deterministic-emitter.ts).
- F-G6 ✓REAL brief-compliance table missing cost-ceiling + 6 water-quality setpoints + turnover → TODO (engine audit + exporter compliance tab).
- F-G7 ✓REAL backup heater sized 100% of duty (should ≤50% standby) + blower kW mismatch → agent (engineering-contract.ts).
- F-G8 ✓REAL drawings density: P&ID 25% / connections 19%, BFD 42%, 26 tags absent → TODO drawing generators (after the re-run, re-render vs corrected state).
- F-G9 ~minor instrument duplicate lines + heat-pump duty-scaling pricing → agents (consolidate + duty-scaled class budget).
- **RESULTS (re-run out/ras-5m, 2026-06-20):** ✓ heater £1.24M→**£49.8k** (219 kW 50% derate + £200/kW floor), ✓ blower emits **2.5 kW**, ✓ **£0 lines = 0** (DN400 valve £1,800), ✓ cost-waterfall factory_cogs **£4.14M** (incl. field-install), installed **£5.03M**. ✓ blower-kW + heater-cable deterministic fails **CLEARED**. ✓ drawings generators fixed (P&ID **100%/92%**, BFD **100%** — swarm's "25%" was a STALE parts-ledger measurement, re-render proved 90%). ✓ per-tank: agent fixed the synthesis (184) + I fixed the **SOURCE** (engineering-contract.ts:11774 hardcoded `tankVolumeEachM3=334` → derive total/count; 204t stays 334×10, 45t→184×4). **2 deterministic fails remain:** (a) per-tank — fixed in code, needs the verify re-run; (b) → **D7**. Swarm-fix set = 9 files, committed; verify re-run in flight.

## Status log
- 2026-06-19 created (captures the full recent discussion). A1/A2 DONE+pushed; A3 in-progress (sub-agent).

## G · LEDGER-DRIVES-CONNECTIONS RE-ARCHITECTURE (Tristan 2026-06-20, "this is a big issue") ⭐🔴
> Tristan: "the ledger should be driving ALL connections — which part connects to what with what. Blender should ONLY do the distance calc from where parts sit. Random pipes nothing→nothing + parts not connecting means the ledger is wrong." CHOSE "Full re-architecture now" (over staged/defer).
> ROOT (proven on out/ras-5m): contract.topology authors **7 edges** (open loop — no recirc pump, UV→nothing); Blender INVENTS the other **99** via augment_topology_connect_orphans + augment_topology_cross_module → the None→None dangling route + dry-ancillary→tank ties. Cost reads Blender's invented schedule. parts-ledger.json already flags n_concerns=2 (UV missing_output, solenoid missing_input) — the ledger KNOWS it's incomplete but Blender papers over it.
- G1 🔨 Build `connection_ledger.py` (shared, position-free) — authors the COMPLETE, endpoint-valid, SIZED graph (every part→what→with-what). Moves augment_* logic out of build_universal_scene. Fix bugs: never emit unresolved-endpoint edge (kill dangling); connect each part to the RIGHT thing (dry feed/mortality/biosecurity ancillaries NOT water-tied to the tank); close the process loop (UV→pump→tanks, incl. recirc pump).
- G2 🔨 Blender consumes the ledger READ-ONLY — remove the 2 augment calls (build_universal_scene 11699-11700), read ledger, resolve endpoints → FAIL-LOUD if unresolved, measure routed length, render. Write authoritative connection-ledger.json.
- G3 🔨 BoM reads the ledger directly — requirements_bom._connection_rows reads connection-ledger.json (+ measured lengths), not Blender's connection-schedule.json.
- G4 🔨 Extend class-connections.ts required-connection registry where the universal completion is too thin.
- G5 🔨 VERIFY no-regression on CO2/SAF/BESS (+ RAS): connectivity n_concerns→0, 0 dangling routes, every part connected, cost sane.
> Builds on G-pre: A/B/C connection SIZING fixes already landed (fb34031e9, e3c6b24d0): branch ties sized to own duty, render ties thinned, CONN3 length-plausibility gate. Those stay (the ledger will author the right sizes; CONN3 still guards geometry).

### G1 progress (2026-06-20): ledger AUTHORITY + strict COMPLETENESS gate landed (commit b4299a164)
- ✓ connection_ledger.py authority: kills nothing→nothing dangling route + 5 dry-ancillary→tank ties; BoM £535k→£480k.
- ✓ STRICT completeness gate (deterministic CONN0): role-aware (flow-through needs in+out; injector/origin out-only; sink in-only; inline-tap ≥1). Now FAILS on real gaps (was 80%-coverage silent-pass).
- 🔨 15 GENUINE gaps the gate now isolates (the closure = next): (a) OPEN LOOP — UV no output + Recirc Pump no input (the recirc cycle isn't closed: needs UV→pump→tanks + pump in the contract); (b) SINK inputs — Bleed/Drain, Solids/Sludge, Effluent need a bleed line FROM the loop; (c) classifier — Aeration Blower=air not water, Biofilm Carrier Media=internal to MBBR, O₂ Solenoid=injector (regex). CLOSURE PLAN: (1) close the main process loop in engineering-contract.ts RAS block (pump + return legs); (2) completion direction-closer wires each sink's input from the loop; (3) classifier: air-movers→'air', internal media→no service (component_engineering._required_services). Then CONN0 → 0 legitimately.

### G1b — UNIVERSAL finding (2026-06-20): the completion is output-only on EVERY archetype
- Proven by running the ledger authority on SAF (out/oxccu-saf-v21, non-RAS): 63 connections authored, 0 integrity violations, trace works (machinery UNIVERSAL ✓) — but completeness flags **49 of 63 SAF parts** missing a connection (vs 15/80 RAS). Same disease everywhere: augment_topology_connect_orphans does `p → module_sink` (OUTPUT only); the promised direction-closer was never built → most parts dead-end on input. THIS is why the graph looks sparse vs the 330-line BoM (which = 138 sub-components + 105 connection lines + 87 principal).
- THE FIX (next increment, UNIVERSAL, must be done CAREFULLY not guessed — a hasty input-guess recreates the 'random pipes' Tristan hates): a flow-derivation direction-closer that chains FLOW-THROUGH parts (vessel/pump/filter, exclude injector/origin/sink/inline-tap/instrument) in PROCESS ORDER within each module (region_rank), first part fed from the module's cross-module upstream, last → sink — so every flow-through part gets a real input from its actual predecessor (serial topology, NOT a star, NOT a guess). Options weighed + rejected: S→p→S recirc-star (wrong on once-through SAF: product-sink→reactor is backwards); blanket input-from-sink (creates wrong edges). The per-class contract loop-closure I added for RAS (UV→pumps→tanks, commit 8c91cb826) is the explicit-correct band-aid for the RAS MAIN loop; the universal closer is what scales to all archetypes.
- The gate (CONN0) now MEASURES this on every class — it is the self-correcting net that drives the closer. Verify target: RAS 15→~0, SAF 49→~0, no new wrong edges (referential integrity stays 0, no detour-ratio blowup).

### G — DEMO-READY MILESTONE (2026-06-20): ledger drives connections, universally, £5M EXACT
- £5M EXACT: out/ras-5m installed **£4,984,339** (55 t/yr, 0.3% under the £5M ceiling); contract loop CLOSED (9 topology edges incl. UV→recirc_pumps→tanks); scorecard quality loop COMPLETE (all sections ≥8/10).
- LEDGER (universal, proven RAS+SAF, integrity 0 both): completeness journey RAS 26→15→7→6, SAF 49→38→9→7. Fixes (~11 commits fb34031e9…f2c6cbf2f): sizing (branch-veto/service-tie/CONN3 length gate) · authority (dangling + dry ties dropped) · STRICT role-aware completeness gate · referential integrity + per-part adjacency + Excel "Connection trace" tab · contract loop-closure + universal direction-closer (input+output) · power vocab + lone-hub (SAF 0→34 power feeds) · classifier exemptions (sub-components/air-movers/injectors) · air-movers→air consumer + sub-components→parent.
- Demo artefact: `~/Downloads/RAS_5M_dossier_2026-06-20_0620.xlsx` (31 tabs incl. Connection trace). Deterministic suite 256/2 (the 2 fails = the 6 boundary completeness gaps [correctly flagged] + the loose-placement length-plausibility [#147]).
- REMAINING (final finer increment, NOT classification errors — genuine BOUNDARY gaps the gate now NAMES): ~6 RAS / 7 SAF — feed-in from the battery-limit origin (SAF feed compressors), product-out (storage), sinks needing their upstream located (RAS bleed/sludge/effluent/expansion/chilling), 1 synthetic duplicate (RAS "Oxygenation System" ≈ oxygen_cones). Need: a battery-limit origin/sink connector + sink-upstream improvement. Plus placement-polish (#147, the loose 53.9 m layout) + the media→MBBR token-match miss.
