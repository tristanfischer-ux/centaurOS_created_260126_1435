# Universal Tool Audit — correctness on realistic engineering inputs

> Generated by `scripts/audit-tools.ts`. audit-tools.ts@v1 — realistic-input correctness audit (not a smoke test).
>
> Each tool is treated as **broken until a realistic self-test proves otherwise** — the
> same discipline the on-the-fly tool-generator applies to a newly generated tool
> (`scripts/lib/orchestrator/generic/tool-generator.ts`), turned around and pointed at the
> existing 182-tool catalogue. For each tool the reasoner
> (`google/gemini-3.1-pro-preview`, temp 0) generated ≥2 realistic test cases (realistic
> physical inputs + plausible `[min,max]` output ranges) from the tool's name/purpose +
> its real input/output field names; each case was run through the tool's python via the
> repo `.venv`; the verdict reuses `tool-generator.ts::runSelfTestCase` verbatim.
>
> This is NOT a fix pass — it is the audit record. Fixing the failing tools is a separate pass.

## Headline counts

| Metric | Count |
| --- | ---: |
| Tools registered | 1 |
| Python tools audited | 1 |
| **PASS** (correct on realistic inputs) | **0** |
| **FAIL** (silently broken on realistic inputs) | **1** |
| Skipped (pure-TS, no python self-test) | 0 |
| **Pass rate of python tools** | **0.0%** |

## Failure breakdown by type

| Failure type | Count |
| --- | ---: |
| Out-of-range value (computed but implausible) | 1 |

## Every FAILED tool (grouped by failure type)

### Out-of-range value (computed but implausible) (1)

| Tool | py_file | cases (pass/run) | Reason |
| --- | --- | ---: | --- |
| `gear-ratio:bicycle` | `gear_ratio.py` | 1/2 | 1/2 case(s) failed; first: "range_km_at_250w" = 162.1 is OUT OF RANGE [80, 150] [case: High-performance electric mountain bike] |

## Appendix — tools that PASSED

_None._
