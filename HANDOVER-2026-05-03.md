# Handover — ForgeOS Engine Pipeline — 3 May 2026

## Current State

The autopilot pipeline infrastructure has been repaired (22 fixes deployed), but the 5 demo projects are stuck at Stage 1 (Chase) with the circuit breaker tripped.

### Project Status
| Project | Stage | Autopilot | Status | Attempts |
|---------|-------|-----------|--------|----------|
| BESS — 40ft 1.5 MWh | design | waiting_chase | manual_review | 4x |
| Containerised desalination | design | waiting_chase | manual_review | 3x |
| HAPS — Wren-style | design | waiting_chase | manual_review | 14x |
| Hedgerow — bird feeder | design | waiting_chase | manual_review | 3x |
| Modular vertical farm | design | waiting_chase | manual_review | 3x |
| 4 other projects | design | (no autopilot state) | — | — |

**Root cause:** Chase research scores consistently 5-7/10 on the 6-model judge panel, triggering the circuit breaker (3 attempts → manual_review). The infrastructure fixes (race conditions, data contracts, quality gates) are working correctly — the circuit breaker IS firing as designed. The problem is Chase's OUTPUT QUALITY, not the pipeline mechanics.

---

## What Was Done This Session

### 1. Council Process Redesigned
- Created `~/.claude/skills/council/SKILL.md` — two-phase protocol (Wide Sweep + Deep Dive)
- Phase 1: 5 workers scan by architectural lens (Concurrency, Data Flow, Resource, Security, Architecture)
- Phase 2: Adversarial pairing (attacker proves bug, resolver proposes fix)
- `.council/` directory initialized in ForgeOS with `known_state.json` + `health_ledger.md`

### 2. 22 Infrastructure Fixes Committed (584ae0c0)

**Race conditions (5):**
- Non-atomic read-modify-write on autopilot_state → partial JSONB updates
- TOCTOU race on pipeline_runs insert → 23505 dedup returns `already_running`
- Zombie pipeline_runs cleanup → added finished_at check before delete
- Cron + after-hop dual-trigger → parallelized with Promise.allSettled
- Gate crash bypass → returns FAIL not WARN

**Data contract fixes (6):**
- STAGE_VALIDATION_MODE wrong stage names → corrected keys
- proofreader_report vs proofread_findings column mismatch → unified
- ScoreBreakdown missing urlShape → added to interface + defaults
- waiting_max_redecomposition missing from STAGE_CONFIG type → removed from Exclude
- STAGE_ORDER missing waiting_max_redecomposition → added
- AutopilotState type casts → `as unknown as Record<string, unknown>`

**Quality gates (3):**
- Gate 1 (brief alignment) — uncommented and implemented
- Gate 4 (cost realism) — new DeterministicGate implementation
- Proofreader blocker findings now halt PDF generation (new `proofreader_blocked` state)

**Kimi improvements (4):**
- Vague brief detection (100-char min + 2 engineering dimensions)
- Source URL validation (HEAD requests, 30% invalid threshold)
- Unique partial index on parts (migration created, NOT applied)
- Supplier URL shape scoring (+5 for /products, -10 for /blog/)

**Cost reductions:**
- MAX_SCORING_ATTEMPTS: 15 → 5 (saves £7.50/project)
- Judge panel lineage monoculture fixed (6/8 stages had duplicate xiaomi)

### 3. Files Modified
```
src/app/api/cron/autopilot-tick/route.ts
src/app/api/autopilot-step/route.ts
src/lib/forge-v2/stage-scoring.ts
src/lib/forge-v2/judge-panels.ts
src/lib/forge-v2/stage-gates/runner.ts
src/lib/forge-v2/stage-gates/gate-1.ts  (uncommented)
src/lib/forge-v2/stage-gates/gate-4.ts  (new)
src/lib/forge-v2/stage-gates/registry.ts
src/lib/forge-v2/stage-config.ts
src/lib/forge-v2/fix-router.ts
src/actions/cad-lab-projects.ts
src/actions/cad-lab-supplier-match.ts
src/actions/specialists/run-chase-research.ts
src/actions/forge-shortlist.ts
src/app/(platform)/the-forge/services/image-generator.ts
supabase/migrations/20260502000000_unique_parts_per_project.sql  (NOT applied)
```

### 4. Analysis Sources Reviewed
- **6-model council** (Gemini coordinator + 5 workers): Found 4 deep deadlock bugs
- **GLM wide sweep** (separate terminal): Found 31 issues across all stages
- **Kimi schematic** (`PDF-Engine-Schematic-Kimi-New.html`): Found ~25 issues with code-level fix suggestions

---

## What Needs To Happen Next

### Priority 1: Chase Research Quality (BLOCKING EVERYTHING)
Chase scores 5-7/10 consistently. The infrastructure is working but the output is too shallow. Options:
1. **Check what Chase is actually producing** — query `cad_lab_projects.research` for the latest Chase output and examine quality
2. **Run the council on Chase specifically** — why does it score low? Missing sources? Shallow analysis? No regulatory detail?
3. **Consider reverting to a simpler prompt** — the structured output requirements may be causing the LLM to produce formulaic content that judges score as "checkbox" rather than genuine research
4. **Check if the 10-model tournament is firing** — it may be that the tournament produces better output but costs more

### Priority 2: Apply Migration
```bash
npx supabase login
npx supabase db push --linked
```

### Priority 3: Remaining Kimi Suggestions (not yet implemented)
- **High:** Early feasibility verdict at sizing stage (stops impossible designs before expensive LLM work)
- **Medium:** Pre-redecomposition snapshot + restore on excessive drift
- **Medium:** Cohort sync check before PDF generation
- **Low:** Currency conversion in Finn costs, Modal health check, retry with relaxed constraints

### Priority 4: AutopilotState Interface Cleanup
The interface is missing runtime fields (status, attempts, total_resets, council_diagnosis, stage_scores). These are accessed via `as unknown as Record<string, unknown>` casts — works but fragile.

---

## Key Files for Next Session
- `src/lib/forge-v2/judge-panels.ts` — judge configuration per stage
- `src/lib/forge-v2/stage-scoring.ts` — scoring logic, thresholds, rubrics
- `src/actions/specialists/run-chase-research.ts` — Chase's research prompt and execution
- `src/app/api/cron/autopilot-tick/route.ts` — cron orchestrator (terminal stages, circuit breaker)
- `PDF-Engine-Schematic-Kimi-New.html` — Kimi's analysis with code suggestions

## Gotchas
- **Chase scoring is the bottleneck, not infrastructure** — all 22 fixes work correctly but can't help if Chase output is genuinely poor
- **Circuit breaker at 3 attempts** — after 3 failed scoring rounds, project goes to manual_review
- **The 4 projects without autopilot_state** were likely created/reset but never had their autopilot started
- **Supabase CLI needs authentication** for `db push` — create access token at supabase.com/dashboard/account/tokens
