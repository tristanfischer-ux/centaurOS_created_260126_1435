# Excel Dossier — Tristan Review Pass 2 (2026-06-21) — FULL SPEC

> Tristan did a complete tab-by-tab + drawing-by-drawing review of `ras-5m-v27_dossier`. This file captures EVERY item (A–W) with the fix, location, and status so the implementation sprint survives context compaction. **All fixes UNIVERSAL** (exporter `scripts/build-excel-export.py` + drawing generators `scripts/blender-universal/draw_*.py` + `build_universal_scene.py`); no RAS-only hacks, no metric-gaming. Run via `generate_drawing_set.py <state.json> <out_dir>` (drawings, standalone, no Blender) → `parts_ledger.py` → `build-excel-export.py <out_dir> <out>/dossier.xlsx`; Blender needs a re-render. Latest run dir: `out/ras-5m-v27`. Drawings render SPARSER in the chain than a standalone re-render — always re-render standalone before measuring.

## CODE ALREADY DONE this session (uncommitted, on disk in build-excel-export.py — COMMIT THESE)
- **A (calc unify)** — `_infer_inputs_from_formula(formula, substitution)` + `_unit_from_symbol` helpers added; wired into tab_calculations (`if not inputs: inferred = _infer_inputs_from_formula(...)`). Recovers named inputs from a legacy calc's formula↔substitution so it renders via the SAME yellow-input+live-result structured path (no more divergent "recomputed live" text block). NEEDS rebuild+verify (route legacy calcs through structured renderer; confirm rhs_of+formula_to_excel bind the inferred symbols, else falls back).
- **C (process-temp matcher)** — `_unit_family` now strips `°` + (STILL TODO) add temperature family keys `c/degc/celsius → ("temp_c",1.0)` so `process_temperature_c` (26.4) matches `water_setpoint_temp_c` (26.4 °C). Only the `°`-strip is in; ADD the temp_c family entries to the `table` dict in `_unit_family`.
- **F (bootstrap stub)** — REMOVED the "Engine bootstrap economics (NOT used)" section from tab_inputs_assumptions.
- **G (charts)** — `style_chart`: axis titles cleared (`ax.title = None`, they overlapped tick labels); data labels = showVal-only with showSerName/showCatName/showLegendKey/showPercent=False (no more "Series1"). STILL TODO: the PIE data labels (in tab_economics) → set showCatName+showPercent, showSerName=False+showVal=False; fix the white/unfilled negative bar (EBITDA Low) fill consistency.
- **L1 backticks** — `clean_cell` now `.replace("`","")` (strips markdown code-ticks `201-PR`→201-PR; applies to all md-parsed schedule tabs).

## REMAINING (the sprint) — by theme

### Theme 1 — Table / schedule hygiene
- **B2 (#187) TAGS — one canonical NUMBERED tag per part, used IDENTICALLY across BOM / Cost basis / Spec sheet / Connection trace / Line list / Valve list / Instrument index / Line&velocity / drawings / flow diagram.** BOM shows BARE ISA letters (LT, TT, AT×6, PT — non-unique) while others have numbered tags (X-127, K-102). Number the instruments (LT-201, AT-203… matching the P&ID instrument-index numbering). Build a name→canonical-tag map and use it everywhere. THIS IS FOUNDATIONAL (underpins D, E, O).
- **L3/M/N TRUNCATION** — Service/Description columns still show "…" on Line list, Valve list, Instrument index, AND the line list "Service" + the from/to. `_render_md_table` has auto-height but it's NOT firing/enough on these tabs — verify WRAP_TOP is applied + row heights set + column wide enough on EVERY md-table tab. Also drawing-level truncation everywhere (see Theme 4).
- **N RANGE column empty** — instrument index Range = "—" for all; the ranges EXIST in the BoM requirement strings ("0–2.4 m", "0–40 °C") — extract + populate.
- **L4/L5/N/V GLOSSARY tab (NEW TAB)** — define EVERY abbreviation: DN (Diameter Nominal/pipe bore), CS (carbon steel), 316L, HDPE/PE100, DUPLEX, PP (insulation), ISA (instrument tag standard), PT/TT/LT/AT/LSL, FC/FO (fail closed/open), PSV/HV/XV, MCB/MCCB, Z-zones (HVAC served zones), DX (dehumidifier), status codes (BESPOKE/UTILITY/SYSTEM/IDENTIFIED/NOT FOUND/INSTRUMENT). Universal — pull the set from the data.
- **K (#195) PANEL SCHEDULE live formulas** — parsed as static text from panel-schedule.md; Design-A, ΔU%, totals are NOT live formulas → unverifiable. Make them real Excel formulas (Design I = kW·1000/(√3·V·pf); ΔU% from CSA/length/current; TOTALS=SUM).
- **B1/J (#191/#196) SUBTITLES** — make each tab's purpose explicit: BOM = procurement BILL (tag + MPN + qty + LINE PRICE + sub-component explosion); Cost basis = per-line cost METHOD (catalogue/llm_estimate/class_reference) + AACE Est-class 1–5 confidence + provenance + install-factor ROLLUP; Spec sheet = per-PRINCIPAL SIZING RATIONALE (the driving worked-calc that sized it + rating + material + MPN).

### Theme 2 — Charts (G mostly done)
- Finish G (pie labels + bar fill, above).
- **I (#194) MORE SCENARIOS on the Scenarios tab** — add what-if sensitivity setups beyond the output sweep + Low/Central/High EBITDA: sale price ±, feed price ±, energy price ±, FCR ±, capex ±, combined best/worst, breakeven price, tornado. Live formulas referencing the Inputs tab drivers.

### Theme 3 — Connection model (the UNIFYING theme — tables + 3D must agree, lengths/costs DERIVED)
- **D (#189) Connection trace — LIVE CELL-REFERENCE links.** Inputs(from)/Outputs(to) cells become `=A{row}` references to the part's OWN row (col A) so Excel Trace Precedents/Dependents works + cells navigate. Add a Tag column. Uses the B2 canonical-tag/name→row map.
- **E (#190) Connection trace FLOW-DIAGRAM IMAGE (NEW).** Generate a directed flow/network diagram from the 103 connections (nodes = parts by tag+name, edges coloured by service: water/power/air/signal), embedded as an image tab, to follow visually. graphviz `dot` NOT installed → deterministic layered SVG (tier by service: sources→distribution→process train→instruments), like draw_bfd.
- **O (#…) Line & velocity schedule** — from/to cell-refs (D) + tags (B2); **O3** check the "out of spec" run near row 106 (confirm real velocity/volt-drop breach, not display bug); **O4** SPLIT the mixed water + electrical into two labelled sections (process-pipe: velocity vs ≤3 m/s; electrical-cable: volt-drop vs ≤5%) — don't mix m³/s + amps + kW in one Rating column; **O5** add a BASIS column showing where Line £ comes from (pipe £/m × length × DN; cable £/m × length × CSA).
- **P (#198) BLENDER — nozzle attachment points + port-to-port routing + floor.** KEY insight: render quality is GOOD (don't touch materials/lighting). Problems: pipes float from nowhere / end mid-air (not terminating on equipment); NO floor/ground slab; the unit-to-unit connections from the tables aren't visibly attached. FIX: each part gets NAMED attachment points (nozzles: water in/out, power, air); every line routes PORT-TO-PORT (nothing floats); line LENGTH = routed port-to-port distance (drives velocity/volt-drop/line-cost). Add a real floor. `build_universal_scene.py` placement+routing. Ties to #122 (one shared spatial source) + #157 (signal ports).
- **Q (part of #198) BLENDER — no part-vomit, no floating lines.** Random little boxes/squares = small/abstract parts (instruments, valves, services, sub-components) rendered as GENERIC CUBES — don't; give real small symbols OR omit from hero (Tristan rejected cube-vomit before). Floating lines = same routing/attachment issue as P.

### Theme 4 — Drawings (ALL `draw_*.py` — one four-part fix applied to every generator)
- **S (#…) A2 MINIMUM sheet size** — all drawings are A3 (GA/HVAC say "1:500 @ A3"); bump sheet format to **A2 (420×594mm)** or larger; scale embed resolution to match (downscale cap already 2600px — may need higher for A2). Applies to GA, P&ID, BFD, single-line, HVAC, isometrics, process-schedules.
- **T/U/V/W LABEL DE-COLLISION** — words hidden behind boxes / other labels (P&ID instrument bubbles `AT AT AT` stacked; BFD "source heat" under the Heat Pump box; HVAC duct labels overlapping zones). Offset labels, leader lines, don't stack.
- **T/U/V/W NO TRUNCATION / NO EDGE-CLIPPING** — "degas water reox…", "…pump…", BFD "n supply feed" (Oxygen supply feed clipped at left edge), HVAC duct schedule "…14.4 …" cut off, isometric (W) bottom title-block/notes truncated. Full text + proper margins.
- **R (#199) GA — multi-column equipment schedule** (72 items in one column overflows + overlaps the title block → 2–3 columns) + plan legibility (small numbered items scattered around the 4 tanks — group/size legibly; placement clarity ties to layout optimiser LAYOUT_OPTIMISE=1).
- Drawings should VISIBLY show the unit-to-unit connections (ties to Theme 3).

### Theme 5 — NEW native sections (Tristan 2026-06-21: "introduce some things that were in the PDF into the spreadsheet — the risk register and the regulatory and compliance parts, but written into the excel, not as pdfs". NOT a priority pivot — do AFTER the A–W sprint, before the final rebuild.)
- **X — RISK REGISTER tab (native rows).** The RAS `state.json` has NO ready risk-register field (the old `src/app/(platform)/the-forge/components/risk-register.tsx` reads the legacy X-ray path, not the universal chain). Build it UNIVERSAL + traceable from TWO live sources: (1) **engine findings already in state** → one risk row each: `costSanity` deviations, `physicsCritique` HIGH/MED, `physicsCriticEnforcement`, `drawingGates` fails, `selfAudit` blocking/low sections, `residualIssues`, `complianceGate` WARN/unverified, connectivity concerns, any "out of spec" line run, scorecard sections < 8 — severity from the finding's own severity, mitigation = its recommended action; (2) **process-hazard rows derived from the equipment PRESENT in the BoM** via a class-agnostic hazard library keyed off BoM/quantity tokens (stored O₂/LOX → oxygen-enrichment fire; ammonia/biofilter → toxic gas; pressurised vessel → stored-energy/PED; rotating machinery/pump → mechanical+recirc-loss; HV switchgear → arc flash; tank/working-at-height → fall/confined-space; bioprocess → pathogen; H₂/flammable → fire/explosion). Columns: ID · category · hazard/finding · cause · severity (1–5) · likelihood (1–5) · risk score (S×L, RAG-coloured) · mitigation · residual · source (live cell/finding ref). NO RAS-only hardcoding — keys off hazards/findings, universal across archetypes.
- **Y — REGULATORY & COMPLIANCE tab (native rows).** Build UNIVERSAL from: (1) **`state.complianceGate`** (verdict / jurisdiction(s) / mandatory_total+covered / conflicts / reason) rendered as a status banner + rows; (2) a **jurisdiction × hazard → regulation matrix** deriving applicable regs from the hazards present + detected jurisdiction (UK e.g. PUWER, Electricity at Work Regs / BS 7671, Pressure Systems Safety Regs 2000 / PER 2016, DSEAR, Environmental Permitting Regs (discharge consent), CDM 2015, Supply of Machinery (Safety) Regs / UKCA, Welfare of Farmed Animals where livestock; map equivalents for US/EU/CA) — each row: regulation · why it applies (the triggering hazard/equipment) · mandatory/advisory · covered? · action; (3) any standards actually cited in the design (jurisdictional-standards data if present). Keys off `complianceGate.jurisdictions_detected` + derived hazards — universal, not per-class. Honest: where `complianceGate.verdict=WARN` ("no class-standards registered"), SHOW that verdict prominently (don't paper over it) + still list the hazard-derived statutory duties that always apply in the jurisdiction.
- **Z — ASSEMBLY & ERECTION SEQUENCE tab (native rows).** (Tristan 2026-06-21, do LATER but tracked.) The PDF carried an assembly/erection sequence; bring it into the Excel as native rows (NOT a PDF image). Find the source first: grep the renderer/state for an erection/assembly/installation-sequence field (likely a `*.md` in the run `drawings/` dir, or a state field like `assemblySequence` / `erectionSequence` / an installation narrative); if none exists for the RAS run, derive a UNIVERSAL sequence from the BoM equipment + the connection graph (civils/foundations → tanks/vessels set → major equipment → pipework → electrical → instrumentation → commissioning), ordered by dependency. Columns: step · activity · equipment/tag involved · predecessor step(s) · plant/lifting need · duration (if derivable) · hold/witness point. Keep universal (derive from equipment + deps), not RAS-only.
- Tasks: #200(X risk register) #201(Y regulatory) #202(Z assembly/erection sequence). Data sources confirmed present in RAS state: `complianceGate`, `costSanity`, `physicsCritique`, `physicsCriticEnforcement`, `drawingGates`, `selfAudit`, `residualIssues`, `requirementsBom`(equipment tokens).

## Tasks in harness: #186(A) #187(B2) #188(C) #189(D) #190(E) #191(B1) #192(G) #193(F) #194(I) #195(K) #196(J) #197(L line list+glossary) #198(P/Q Blender) #199(R GA). Plus: S(A2), O(line&velocity), T/U/V/W (drawing de-collision+truncation — fold into the draw_*.py workstream).

## SPRINT ORDER (recommended)
1. Commit the done code (A/C-partial/F/G-partial/backticks). 2. Finish exporter quick wins: C temp family, G pie+bar, B1/J subtitles, L3 truncation, N range. 3. B2 canonical tags (foundational) → D cell-links → O split+basis. 4. Glossary tab. 5. K panel formulas. 6. I scenarios. 7. E flow-diagram SVG. 8. Drawings: A2 + de-collision + no-truncation + GA columns across draw_*.py. 9. Blender P/Q: ports + floor + port-to-port routing (re-render). 9.5. NEW native tabs X (risk register) + Y (regulatory & compliance) + Z (assembly & erection sequence) — Theme 5. 10. Standalone re-render drawings → parts_ledger → rebuild Excel → reopen. Verify 0 CHECKS FAIL + look at every tab/drawing.
