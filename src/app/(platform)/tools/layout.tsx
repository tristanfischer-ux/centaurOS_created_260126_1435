/**
 * @description Layout for the /tools section. Minimal wrapper —
 * the platform layout already provides sidebar and padding.
 */
export default function ToolsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
