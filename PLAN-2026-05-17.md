# ForgeOS — fix-everything plan, 2026-05-17

> Single source of truth for every open problem and the fix order. Approved by Tristan in plain text: just-do-it mode, no more permission loops. Each block is a unit of dispatch — sub-agent or main-thread.

## Decisions locked

- **Corpus target: 60 classes.** 15/60 done (10 original + 5 Tier 1 batch A).
- **Source-of-truth fixes deferred and batched** every 10 new classes.
- **Phase 3 must finish before Tier 2 begins.** Tesla validation is in flight.
- **Sectors added to roadmap**: space (satellites, rockets, ground stations, launch vehicles) AND consumer electronics (smartphone, smart speaker, wearable, console, smart-home hub). Defence stays OUT.

## Open problems — full inventory

### A. PDF quality residuals (phase20 audit: 11 MAJOR + 34 MINOR)

After the phase20 fix-pass killed all 3 BLOCKERs and 78% of MAJORs, what's left:

- **A1** — 9 of 11 MAJORs were "missing exploded-view PNG" — already fixed in phase21 (9 PNGs Blender-rendered, 9 phase21 PDFs re-rendered). ✅ DONE.
- **A2** — 1 MAJOR is a Portobello-RMF false-positive in the audit script itself (renderer is correct). ✅ NO-OP.
- **A3** — 1 MAJOR is a PyMuPDF hyphenation artefact (`Iso-\nlation` mis-extracted as `Iso`). PDF itself is correct. ✅ NO-OP.
- **A4** — 34 MINORs across all 10 products: typography nits, occasional snake_case leaks in deep nested fields, a few US-spelling escapes the britishise() regex didn't catch, sub-optimal line breaks, residual stray glyphs.

**Action**: dispatch a 34-MINOR sweep agent. Cost ~£0.20. Re-render to phase22 if needed.

### B. Phase 3 reverse-engineering reproduction (the "pretty bad" result)

What we know:
- Original Phase 3: 3 fresh runs, 27% sub-module recall (vs 39-79% Phase 2 averages). Verdict: pipeline gap, not scorer artefact.
- 4 surgical fixes diagnosed + applied: (1) class-module-priors moved mass_fluid from forbidden to optional for BESS, (2) prompts.ts worked-example rewritten with cross-module grammar_link explanation, (3) container_shell synonym bucket extended, (4) BESS safety/sensing vocab buckets added.
- Tesla Megapack validation run in flight (~45 min). Watcher will fire when complete. Predicted lift: 23.4% → ~45%.

**Action chain**:
- **B1** — wait for Tesla validation (in flight, no action)
- **B2** — if Tesla ≥40% (PASS): apply same pattern to Saab AUV + Sartorius bioreactor via their class-module-priors + worked-example fixes. Validate by re-running Saab + Sartorius. Cost ~£8.
- **B3** — if Tesla <40% (FAIL): deeper investigation — UNION synthesis stage may be voting on wrong-but-popular emitter outputs. Council the synthesis logic. Cost ~£3 (council only).
- **B4** — wire V4 distributor-cascade verification into fresh-pipeline path so BoM costs land in reference price bands (Tesla pipeline currently under-estimates by 30-90%). Engineering task, ~£3-5 of LLM calls + 2-3 hours of code edits.

### C. Supplier quality (Tristan flagged heatpump specifically as "still not working")

What's actually broken:
- Heatpump phase20+phase21 has 3 archetypes — principal_oem (3 web-fallback names like "Clade Engineering"), compressor_supplier (Copeland, Danfoss), sheet_metal_enclosure (JC Metalworks etc).
- "Still weak" probably means: brand names extracted but the supplier card isn't useful — no contact details, no concrete capability claim, no "why this fits" reasoning grounded in the brief.

**Action**:
- **C1** — extend each supplier card to include: city, country, employee-count band (from forge-truth.db when available; "?" otherwise), one-line concrete capability ("R290 propane heat-pump compressors"), three-bullet "why this fits the brief", and a clear call-to-action ("Contact via website / [phone] / [email]"). Renderer-side edit + enrich-state-with-suppliers-side LLM call (use Flash-Lite for the "why this fits" — ~£0.0002 per candidate × 90 = £0.02).
- **C2** — heatpump-specific: re-run the Brave search with broader queries (already done in supplier-cleanup pass but didn't apply to heatpump specifically — re-do).
- **C3** — supplier confidence: surface DB-verified vs web-fallback as a clear visual badge in the card (already partly there; tighten the styling).

### D. Design Decisions section UX (Tristan: "you recommend then we should just do")

Current behaviour: section lists open decisions with a question + a recommendation + supporting reasoning. Tristan's read: "why are we recommending without then ACTING on the recommendation?"

Two interpretations:
- **D-i (Tristan's preferred)**: each decision should be presented as ALREADY MADE (we picked option X for these reasons, here are the consequences). Implementation IS the next stage.
- **D-ii (current behaviour)**: each decision is OPEN, recommendation is provisional, founder approves/rejects.

D-i is the more useful framing for an engineering report. Founders want decisions they can sign off on, not a list of open questions.

**Action**:
- **D1** — rewire DesignDecisionsPage: each entry presents as "Decision: X. Why: Y. Consequences: Z." Mark all entries as `status: 'recommended_for_adoption'`. Renderer-side edit only — the underlying state data unchanged.
- **D2** — Stage 1.X emitter prompt: shift from "list open decisions" to "make recommendations with explicit reasoning". Pipeline-side edit.

### E. Parts Pending Verification — move to appendix

Currently §5 (mid-report), 28 rows per page, often 3-4 pages of unverified rows. Tristan: too long, not useful at this stage, appendix it.

**Action**:
- **E1** — move PartsPendingVerificationPage to AFTER §7 Suppliers, label as "Appendix A — Parts Pending Verification". Renderer-side reorder + label edit.
- **E2** — add a one-line preview earlier in the report ("12 part lines are unverified; see Appendix A for the audit trail") so the reader knows it exists without reading 3 pages of it.

### F. Corpus expansion plan (10→60 classes, with space + consumer electronics added)

15/60 done. Remaining 45 classes split across:

- **Tier 1 (adjacent, ready to ingest immediately after Tesla validation passes)** — 5 more in R-B batch: marine ESS, second-life battery pack, AC motor controller, mini-split heatpump, custom hybrid drone
- **Tier 2 (medium — robotics / medical / aerospace / marine / manufacturing)** — 10 classes: AMR, AGV, robot arm, insulin pump, dialysis machine, small satellite (NEW: SPACE), ground station (NEW: SPACE), USV, 3D printer, CNC mill
- **Tier 3 (adjacent verticals)** — 10 classes: lift/elevator, escalator, BMS (building management), brewery vessel, sterile fill line, substation skid, transformer, switchgear, telehandler, mini-excavator
- **Tier 4 (sector expansion)** — 15 classes: 
  - **Space (5 NEW)**: cubesat, microsat, propulsion test rig, launch vehicle stage, payload fairing
  - **Consumer electronics (5 NEW)**: smartphone, smart speaker, wearable consumer fitness tracker, gaming console, smart home hub
  - **Biotech/semicon (5 frontier)**: pipettor, microscope, lithography step-and-scan, vacuum chamber, CMP polisher
- **Tier 5 (final 5)**: pick by demand after Tier 4 surfaces gaps

Each class: £0.042 ingestion + boundary discipline + 10 reference products at judge_score ≥ 6. Total: 45 × £0.042 = £1.89 for all remaining ingestion.

**Action**: not now. Gated on Phase 3 Path A verdict. When Tesla validates, dispatch Tier 1 R-B batch (5 classes, £0.21).

## Execution order

### Right now (parallel, while Tesla pipeline still running)

1. **Group A4** — sweep the 34 MINORs from phase20 audit, re-render to phase22 if needed. Sub-agent. ~£0.20.
2. **Group D + E** — redesign Design Decisions UX (recommendation→decision-made) AND move Parts Pending Verification to appendix. Renderer edit, dispatched together. Sub-agent. £0.
3. **Group C** — supplier card upgrade: add city/country/capability/why-fits/CTA + heatpump-specific re-search. Sub-agent. ~£0.20.

These three sub-agents dispatch immediately — they don't depend on Phase 3 Tesla result.

### When Tesla validation lands

4. **Group B2 or B3** — depending on Tesla recall: either propagate the 4 fixes to Saab + Sartorius (PASS path) or council the UNION synthesis logic (FAIL path).
5. **Group B4** — wire V4 distributor cascade into fresh pipeline. Engineering task, ~£3-5.
6. **Tier 1 R-B batch (5 classes)** — ingest marine ESS, second-life battery pack, AC motor controller, mini-split heatpump, custom hybrid drone. £0.21.

### Later

7. **Tier 2 corpus expansion** — 10 classes incl. small satellite + ground station from the space sector.
8. **Tier 3** — 10 adjacent-vertical classes.
9. **Tier 4** — 15 sector-expansion classes (5 space + 5 consumer electronics + 5 biotech/semicon).
10. **Source-of-truth batched fix** — after every 10 new classes, batched ontology + standards + synonym-bucket fixes to class-*.ts.

## Cost budget

- Session so far: ~£20 LLM cumulative (Phase 3 dominated at £15)
- Tesla validation: ~£4 (in flight)
- Group A4 + C + D + E sub-agents: ~£0.50 combined
- Path A propagation (B2 or B3): ~£5-8
- V4 cascade wire-in (B4): ~£3-5
- All remaining corpus expansion (45 classes): £1.89
- Estimated total session: £30-40. Within sensible budget for the day.

## Confidence rating

- **High confidence** on Groups A4, C, D, E — these are deterministic renderer/script edits with clear test criteria.
- **Moderate confidence** on Group B — pipeline diagnosis surfaced specific named fixes but the multi-emitter ensemble's behaviour is partly statistical, so Tesla validation tells us how close we are to ground truth.
- **Moderate confidence** on the 60-class target — gated on B passing. If B fails, methodology questions reopen and we may need to drop the target or extend timeline.
